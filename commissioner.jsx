// ╔══════════════════════════════════════════════════════════════╗
// ║  PlAIncy — Borehamwood Plancy League                         ║
// ║  Generated by scripts/build-artifact.js — all data inlined  ║
// ║  Edit commissioner.template.jsx, then run build-artifact.js  ║
// ╚══════════════════════════════════════════════════════════════╝

// The current Claude.ai artifact runtime is an ESM sandbox — React is imported,
// not a script-tag global. (Older runtimes exposed a global `React`.)
import React, { useState, useEffect, useRef, useMemo } from "react";
const DEBUG = false; // set true to log system-prompt sizes to the console
// Single position→colour map (was duplicated as POSColors + POS_TINT).
const POS_COLORS = { QB: '#ff7a1a', RB: '#10b981', WR: '#60a5fa', TE: '#a855f7', K: '#94a3b8', DEF: '#f87171', FLEX: '#f59e0b' };

const ALIAS = { allyl900: 'AlastairL' };
function canonical(handle) {
  const c = String(handle || '').replace(/^@/, '').trim();
  return ALIAS[c] || c;
}
const NAMES = { AlastairL:'Alastair', dpol:'Dan', saulgoat:'Saul', sanfbe:'Benjy', joshjr11:'Josh', drjkay:'Jamie', GSac:'Gideon', benjlev:'Benjy' };
const DISAMBIG = { benjlev:'Lev', sanfbe:'Sanford' };
function displayName(h) { const c = canonical(h); return NAMES[c] || c; }
function distinctName(h) { const c = canonical(h); return DISAMBIG[c] || NAMES[c] || c; }
function teamLabel(k) {
  const m = String(k).match(/\(@([^)]+)\)/);
  const handle = m ? m[1] : null;
  const team = String(k).replace(/\s*\(@[^)]+\)\s*$/, '').trim();
  const nm = handle ? displayName(handle) : null;
  return (nm && nm !== handle) ? `${nm} · ${team}` : (team || k);
}

const BUILT_AT = "2026-07-21T15:34:57.934Z";

function fmtBuiltAt(iso) {
  try {
    const d = new Date(iso);
    const months = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
    return d.getDate() + ' ' + months[d.getMonth()] + ' ' + d.getFullYear();
  } catch(e) { return 'recently'; }
}

const HISTORY_DATA = {"seasons":[{"season":"2023","name":"Borehamwood","champion":"saulgoat","standings":[{"manager":"drjkay","team":"Obi-Quan Kenobi","wins":10,"losses":4,"pf":1805.28,"pa":1782.94,"high":154.7},{"manager":"joshjr11","team":"Denver Brochos","wins":10,"losses":4,"pf":1795.48,"pa":1721.18,"high":179.48},{"manager":"saulgoat","team":"Saul","wins":7,"losses":7,"pf":1855.7,"pa":1728.1,"high":189.76},{"manager":"sanfbe","team":"sanfbe","wins":7,"losses":7,"pf":1726.64,"pa":1745.1,"high":180.98},{"manager":"allyl900","team":"allyl900","wins":6,"losses":8,"pf":1756.06,"pa":1719.54,"high":151.4},{"manager":"dpol","team":"dpol","wins":6,"losses":8,"pf":1718.68,"pa":1638.22,"high":173.18},{"manager":"GSac","team":"GSac","wins":6,"losses":8,"pf":1582.04,"pa":1745.12,"high":146.14},{"manager":"benjlev","team":"benjlev","wins":4,"losses":10,"pf":1571.92,"pa":1731.6,"high":144.1}],"games":[{"week":1,"playoff":false,"a":"dpol","b":"allyl900","pa":69,"pb":120.54},{"week":1,"playoff":false,"a":"benjlev","b":"saulgoat","pa":116.34,"pb":146.08},{"week":1,"playoff":false,"a":"joshjr11","b":"sanfbe","pa":97.86,"pb":92.34},{"week":1,"playoff":false,"a":"drjkay","b":"GSac","pa":138.16,"pb":103.54},{"week":2,"playoff":false,"a":"benjlev","b":"sanfbe","pa":144.1,"pb":110.76},{"week":2,"playoff":false,"a":"dpol","b":"saulgoat","pa":111.02,"pb":82.48},{"week":2,"playoff":false,"a":"allyl900","b":"GSac","pa":105.66,"pb":110.14},{"week":2,"playoff":false,"a":"joshjr11","b":"drjkay","pa":179.48,"pb":138},{"week":3,"playoff":false,"a":"dpol","b":"sanfbe","pa":173.18,"pb":151.32},{"week":3,"playoff":false,"a":"drjkay","b":"allyl900","pa":136.46,"pb":147.26},{"week":3,"playoff":false,"a":"saulgoat","b":"GSac","pa":125.18,"pb":111.26},{"week":3,"playoff":false,"a":"joshjr11","b":"benjlev","pa":109.28,"pb":130.88},{"week":4,"playoff":false,"a":"benjlev","b":"drjkay","pa":105.6,"pb":132.88},{"week":4,"playoff":false,"a":"joshjr11","b":"allyl900","pa":141.94,"pb":113.88},{"week":4,"playoff":false,"a":"dpol","b":"GSac","pa":99.36,"pb":123.6},{"week":4,"playoff":false,"a":"saulgoat","b":"sanfbe","pa":97.56,"pb":168.6},{"week":5,"playoff":false,"a":"joshjr11","b":"dpol","pa":54.84,"pb":159.32},{"week":5,"playoff":false,"a":"saulgoat","b":"drjkay","pa":189.76,"pb":76},{"week":5,"playoff":false,"a":"GSac","b":"sanfbe","pa":101.78,"pb":130.36},{"week":5,"playoff":false,"a":"benjlev","b":"allyl900","pa":89.84,"pb":113.32},{"week":6,"playoff":false,"a":"benjlev","b":"GSac","pa":130.94,"pb":102.12},{"week":6,"playoff":false,"a":"dpol","b":"drjkay","pa":120.3,"pb":128.18},{"week":6,"playoff":false,"a":"allyl900","b":"sanfbe","pa":102.98,"pb":114.96},{"week":6,"playoff":false,"a":"joshjr11","b":"saulgoat","pa":101.02,"pb":152.3},{"week":7,"playoff":false,"a":"saulgoat","b":"allyl900","pa":121.44,"pb":145.34},{"week":7,"playoff":false,"a":"benjlev","b":"dpol","pa":86.46,"pb":87.56},{"week":7,"playoff":false,"a":"joshjr11","b":"GSac","pa":128.98,"pb":94.18},{"week":7,"playoff":false,"a":"drjkay","b":"sanfbe","pa":132.66,"pb":94.3},{"week":8,"playoff":false,"a":"dpol","b":"sanfbe","pa":121.16,"pb":126.56},{"week":8,"playoff":false,"a":"benjlev","b":"drjkay","pa":86.6,"pb":122.92},{"week":8,"playoff":false,"a":"joshjr11","b":"saulgoat","pa":110.1,"pb":132.68},{"week":8,"playoff":false,"a":"allyl900","b":"GSac","pa":127.96,"pb":134.48},{"week":9,"playoff":false,"a":"benjlev","b":"dpol","pa":117,"pb":114.18},{"week":9,"playoff":false,"a":"saulgoat","b":"GSac","pa":127.82,"pb":123.48},{"week":9,"playoff":false,"a":"allyl900","b":"sanfbe","pa":92.52,"pb":100.12},{"week":9,"playoff":false,"a":"joshjr11","b":"drjkay","pa":120.66,"pb":89.54},{"week":10,"playoff":false,"a":"saulgoat","b":"allyl900","pa":134.18,"pb":142.86},{"week":10,"playoff":false,"a":"dpol","b":"drjkay","pa":131.34,"pb":146.02},{"week":10,"playoff":false,"a":"GSac","b":"sanfbe","pa":135.02,"pb":69.88},{"week":10,"playoff":false,"a":"joshjr11","b":"benjlev","pa":138.46,"pb":113.82},{"week":11,"playoff":false,"a":"benjlev","b":"allyl900","pa":78.68,"pb":130},{"week":11,"playoff":false,"a":"joshjr11","b":"dpol","pa":105.56,"pb":122.74},{"week":11,"playoff":false,"a":"saulgoat","b":"sanfbe","pa":116.54,"pb":126.7},{"week":11,"playoff":false,"a":"drjkay","b":"GSac","pa":154.7,"pb":109.56},{"week":12,"playoff":false,"a":"joshjr11","b":"GSac","pa":150.74,"pb":97.08},{"week":12,"playoff":false,"a":"saulgoat","b":"drjkay","pa":132.28,"pb":132.66},{"week":12,"playoff":false,"a":"dpol","b":"allyl900","pa":144.3,"pb":129.74},{"week":12,"playoff":false,"a":"benjlev","b":"sanfbe","pa":124.52,"pb":133.86},{"week":13,"playoff":false,"a":"benjlev","b":"GSac","pa":87,"pb":98.56},{"week":13,"playoff":false,"a":"dpol","b":"saulgoat","pa":106.04,"pb":137.18},{"week":13,"playoff":false,"a":"drjkay","b":"sanfbe","pa":125.18,"pb":118.36},{"week":13,"playoff":false,"a":"joshjr11","b":"allyl900","pa":168.56,"pb":151.4},{"week":14,"playoff":false,"a":"dpol","b":"GSac","pa":109.08,"pb":106.54},{"week":14,"playoff":false,"a":"benjlev","b":"saulgoat","pa":124.64,"pb":128.12},{"week":14,"playoff":false,"a":"joshjr11","b":"sanfbe","pa":140.74,"pb":125.42},{"week":14,"playoff":false,"a":"drjkay","b":"allyl900","pa":114.94,"pb":118.2},{"week":15,"playoff":true,"a":"allyl900","b":"sanfbe","pa":130.26,"pb":81.66},{"week":15,"playoff":true,"a":"dpol","b":"saulgoat","pa":88.12,"pb":113.32},{"week":15,"playoff":true,"a":"benjlev","b":"GSac","pa":91.1,"pb":107.24},{"week":16,"playoff":true,"a":"drjkay","b":"allyl900","pa":119.24,"pb":127.12},{"week":16,"playoff":true,"a":"joshjr11","b":"saulgoat","pa":97.42,"pb":118.3},{"week":16,"playoff":true,"a":"dpol","b":"sanfbe","pa":91.44,"pb":180.98},{"week":17,"playoff":true,"a":"saulgoat","b":"allyl900","pa":163.68,"pb":85.72},{"week":17,"playoff":true,"a":"joshjr11","b":"drjkay","pa":106.5,"pb":114.56}]},{"season":"2024","name":"Borehamwood","champion":"saulgoat","standings":[{"manager":"saulgoat","team":"Chase waddle waddle ","wins":11,"losses":3,"pf":1903.3,"pa":1711.8,"high":196.9},{"manager":"AlastairL","team":"Fourth and Goalda Meir","wins":10,"losses":4,"pf":1968.96,"pa":1730,"high":178.94},{"manager":"dpol","team":"Plancey Neutral","wins":9,"losses":5,"pf":1732.22,"pa":1716.86,"high":189},{"manager":"joshjr11","team":"Denver Brochos","wins":8,"losses":6,"pf":1713.02,"pa":1709.2,"high":181.32},{"manager":"GSac","team":"War & Childbirth","wins":7,"losses":7,"pf":1758.44,"pa":1729.02,"high":159.22},{"manager":"drjkay","team":"To infinity and Bijan","wins":6,"losses":8,"pf":1767.8,"pa":1761.36,"high":213.46},{"manager":"sanfbe","team":"The Twinder Swindler","wins":5,"losses":9,"pf":1504.7,"pa":1656.34,"high":144.4},{"manager":"benjlev","team":"benjlev","wins":0,"losses":14,"pf":1522.48,"pa":1856.34,"high":142.9}],"games":[{"week":1,"playoff":false,"a":"dpol","b":"drjkay","pa":127.86,"pb":119.98},{"week":1,"playoff":false,"a":"saulgoat","b":"sanfbe","pa":115.76,"pb":108.38},{"week":1,"playoff":false,"a":"benjlev","b":"AlastairL","pa":108.34,"pb":138.28},{"week":1,"playoff":false,"a":"joshjr11","b":"GSac","pa":102.32,"pb":99.52},{"week":2,"playoff":false,"a":"dpol","b":"saulgoat","pa":146.7,"pb":113.1},{"week":2,"playoff":false,"a":"drjkay","b":"sanfbe","pa":153.24,"pb":88.96},{"week":2,"playoff":false,"a":"joshjr11","b":"AlastairL","pa":181.32,"pb":104.26},{"week":2,"playoff":false,"a":"benjlev","b":"GSac","pa":97.84,"pb":115.88},{"week":3,"playoff":false,"a":"dpol","b":"sanfbe","pa":92.36,"pb":126.04},{"week":3,"playoff":false,"a":"saulgoat","b":"drjkay","pa":121.02,"pb":109.58},{"week":3,"playoff":false,"a":"AlastairL","b":"GSac","pa":116.88,"pb":108.68},{"week":3,"playoff":false,"a":"joshjr11","b":"benjlev","pa":137.14,"pb":106.28},{"week":4,"playoff":false,"a":"dpol","b":"AlastairL","pa":142.08,"pb":138.32},{"week":4,"playoff":false,"a":"benjlev","b":"drjkay","pa":125,"pb":129.48},{"week":4,"playoff":false,"a":"joshjr11","b":"saulgoat","pa":109.32,"pb":118.9},{"week":4,"playoff":false,"a":"GSac","b":"sanfbe","pa":129.72,"pb":84.1},{"week":5,"playoff":false,"a":"benjlev","b":"dpol","pa":104.34,"pb":144.96},{"week":5,"playoff":false,"a":"drjkay","b":"AlastairL","pa":101.46,"pb":159.62},{"week":5,"playoff":false,"a":"saulgoat","b":"GSac","pa":137.24,"pb":159.22},{"week":5,"playoff":false,"a":"joshjr11","b":"sanfbe","pa":77.04,"pb":97.24},{"week":6,"playoff":false,"a":"joshjr11","b":"dpol","pa":109.86,"pb":118.3},{"week":6,"playoff":false,"a":"drjkay","b":"GSac","pa":160.76,"pb":135.62},{"week":6,"playoff":false,"a":"saulgoat","b":"AlastairL","pa":145.72,"pb":120.06},{"week":6,"playoff":false,"a":"benjlev","b":"sanfbe","pa":83.22,"pb":112.7},{"week":7,"playoff":false,"a":"dpol","b":"GSac","pa":98.66,"pb":145.04},{"week":7,"playoff":false,"a":"joshjr11","b":"drjkay","pa":119.66,"pb":116.9},{"week":7,"playoff":false,"a":"benjlev","b":"saulgoat","pa":109.06,"pb":119.3},{"week":7,"playoff":false,"a":"AlastairL","b":"sanfbe","pa":121.94,"pb":110.12},{"week":8,"playoff":false,"a":"dpol","b":"drjkay","pa":108.64,"pb":124.9},{"week":8,"playoff":false,"a":"saulgoat","b":"sanfbe","pa":135.44,"pb":98.42},{"week":8,"playoff":false,"a":"benjlev","b":"AlastairL","pa":104.38,"pb":163.96},{"week":8,"playoff":false,"a":"joshjr11","b":"GSac","pa":153.34,"pb":115.86},{"week":9,"playoff":false,"a":"dpol","b":"saulgoat","pa":128.4,"pb":101.22},{"week":9,"playoff":false,"a":"drjkay","b":"sanfbe","pa":142.5,"pb":144.4},{"week":9,"playoff":false,"a":"joshjr11","b":"AlastairL","pa":123,"pb":150.76},{"week":9,"playoff":false,"a":"benjlev","b":"GSac","pa":114.34,"pb":141.68},{"week":10,"playoff":false,"a":"dpol","b":"sanfbe","pa":107.4,"pb":91.5},{"week":10,"playoff":false,"a":"saulgoat","b":"drjkay","pa":134.44,"pb":130.24},{"week":10,"playoff":false,"a":"AlastairL","b":"GSac","pa":128.02,"pb":127.42},{"week":10,"playoff":false,"a":"joshjr11","b":"benjlev","pa":124.08,"pb":123.44},{"week":11,"playoff":false,"a":"dpol","b":"AlastairL","pa":86.38,"pb":178.94},{"week":11,"playoff":false,"a":"benjlev","b":"drjkay","pa":88.64,"pb":104.8},{"week":11,"playoff":false,"a":"joshjr11","b":"saulgoat","pa":150.34,"pb":196.9},{"week":11,"playoff":false,"a":"GSac","b":"sanfbe","pa":128.26,"pb":93.18},{"week":12,"playoff":false,"a":"benjlev","b":"dpol","pa":116.96,"pb":133.08},{"week":12,"playoff":false,"a":"drjkay","b":"AlastairL","pa":116.16,"pb":175.4},{"week":12,"playoff":false,"a":"saulgoat","b":"GSac","pa":162.92,"pb":106.62},{"week":12,"playoff":false,"a":"joshjr11","b":"sanfbe","pa":91.76,"pb":85.06},{"week":13,"playoff":false,"a":"joshjr11","b":"dpol","pa":112.52,"pb":170.18},{"week":13,"playoff":false,"a":"drjkay","b":"GSac","pa":152.2,"pb":110.78},{"week":13,"playoff":false,"a":"saulgoat","b":"AlastairL","pa":132.66,"pb":111.96},{"week":13,"playoff":false,"a":"benjlev","b":"sanfbe","pa":117.14,"pb":122.32},{"week":14,"playoff":false,"a":"dpol","b":"GSac","pa":127.22,"pb":134.14},{"week":14,"playoff":false,"a":"joshjr11","b":"drjkay","pa":121.32,"pb":105.6},{"week":14,"playoff":false,"a":"benjlev","b":"saulgoat","pa":123.5,"pb":168.68},{"week":14,"playoff":false,"a":"AlastairL","b":"sanfbe","pa":160.56,"pb":142.28},{"week":15,"playoff":true,"a":"joshjr11","b":"GSac","pa":130.3,"pb":127.04},{"week":15,"playoff":true,"a":"dpol","b":"drjkay","pa":189,"pb":116.68},{"week":15,"playoff":true,"a":"benjlev","b":"sanfbe","pa":119.76,"pb":137.78},{"week":16,"playoff":true,"a":"joshjr11","b":"saulgoat","pa":94.24,"pb":124.94},{"week":16,"playoff":true,"a":"dpol","b":"AlastairL","pa":141.88,"pb":176.68},{"week":16,"playoff":true,"a":"drjkay","b":"GSac","pa":192.72,"pb":111.9},{"week":17,"playoff":true,"a":"saulgoat","b":"AlastairL","pa":149.16,"pb":143.08},{"week":17,"playoff":true,"a":"benjlev","b":"sanfbe","pa":131.44,"pb":108.78}]},{"season":"2025","name":"Borehamwood","champion":"AlastairL","standings":[{"manager":"dpol","team":"Plancey Neutral","wins":11,"losses":3,"pf":1878.92,"pa":1696.02,"high":186.44},{"manager":"AlastairL","team":"Fourth and Goalda Meir","wins":9,"losses":5,"pf":1828.36,"pa":1785.46,"high":156.58},{"manager":"sanfbe","team":"J’Allen Plancey z’l","wins":8,"losses":6,"pf":1778.06,"pa":1854.68,"high":163.6},{"manager":"saulgoat","team":"Love Thy Naber","wins":8,"losses":6,"pf":1689.66,"pa":1588.06,"high":172.42},{"manager":"joshjr11","team":"Denver Brochos","wins":6,"losses":8,"pf":1818.96,"pa":1792.34,"high":173.36},{"manager":"drjkay","team":"A rookie error","wins":6,"losses":8,"pf":1770.74,"pa":1824.38,"high":169.02},{"manager":"GSac","team":"This One Really Hurts","wins":5,"losses":9,"pf":1786.8,"pa":1833.52,"high":202.36},{"manager":"benjlev","team":"benjlev","wins":3,"losses":11,"pf":1562.56,"pa":1739.6,"high":163.26}],"games":[{"week":1,"playoff":false,"a":"dpol","b":"drjkay","pa":107.02,"pb":99.74},{"week":1,"playoff":false,"a":"saulgoat","b":"AlastairL","pa":88.72,"pb":95.08},{"week":1,"playoff":false,"a":"joshjr11","b":"benjlev","pa":91.32,"pb":163.26},{"week":1,"playoff":false,"a":"GSac","b":"sanfbe","pa":112.08,"pb":155.26},{"week":2,"playoff":false,"a":"dpol","b":"saulgoat","pa":138.08,"pb":137.6},{"week":2,"playoff":false,"a":"drjkay","b":"AlastairL","pa":126.08,"pb":133.88},{"week":2,"playoff":false,"a":"joshjr11","b":"GSac","pa":127.64,"pb":101.44},{"week":2,"playoff":false,"a":"benjlev","b":"sanfbe","pa":109.9,"pb":111.72},{"week":3,"playoff":false,"a":"dpol","b":"AlastairL","pa":131.86,"pb":131.84},{"week":3,"playoff":false,"a":"saulgoat","b":"drjkay","pa":137.62,"pb":113.4},{"week":3,"playoff":false,"a":"joshjr11","b":"sanfbe","pa":90.18,"pb":135.02},{"week":3,"playoff":false,"a":"benjlev","b":"GSac","pa":96.92,"pb":115.04},{"week":4,"playoff":false,"a":"joshjr11","b":"dpol","pa":141.88,"pb":154.2},{"week":4,"playoff":false,"a":"benjlev","b":"drjkay","pa":88.98,"pb":169.02},{"week":4,"playoff":false,"a":"saulgoat","b":"GSac","pa":131.28,"pb":121.2},{"week":4,"playoff":false,"a":"AlastairL","b":"sanfbe","pa":140.52,"pb":151.26},{"week":5,"playoff":false,"a":"benjlev","b":"dpol","pa":135.16,"pb":136.42},{"week":5,"playoff":false,"a":"joshjr11","b":"drjkay","pa":145.72,"pb":102.04},{"week":5,"playoff":false,"a":"saulgoat","b":"sanfbe","pa":140.54,"pb":138.42},{"week":5,"playoff":false,"a":"AlastairL","b":"GSac","pa":127.72,"pb":121.5},{"week":6,"playoff":false,"a":"dpol","b":"GSac","pa":128.38,"pb":118.42},{"week":6,"playoff":false,"a":"drjkay","b":"sanfbe","pa":108.74,"pb":163.6},{"week":6,"playoff":false,"a":"joshjr11","b":"saulgoat","pa":137.16,"pb":106.96},{"week":6,"playoff":false,"a":"benjlev","b":"AlastairL","pa":103.54,"pb":115.04},{"week":7,"playoff":false,"a":"dpol","b":"sanfbe","pa":186.44,"pb":77.44},{"week":7,"playoff":false,"a":"drjkay","b":"GSac","pa":118.16,"pb":192.04},{"week":7,"playoff":false,"a":"benjlev","b":"saulgoat","pa":79.32,"pb":86.94},{"week":7,"playoff":false,"a":"joshjr11","b":"AlastairL","pa":119.98,"pb":156.58},{"week":8,"playoff":false,"a":"dpol","b":"drjkay","pa":147.16,"pb":118.52},{"week":8,"playoff":false,"a":"saulgoat","b":"AlastairL","pa":108.88,"pb":123.88},{"week":8,"playoff":false,"a":"joshjr11","b":"benjlev","pa":146.48,"pb":154.34},{"week":8,"playoff":false,"a":"GSac","b":"sanfbe","pa":145.66,"pb":116.62},{"week":9,"playoff":false,"a":"dpol","b":"saulgoat","pa":79.9,"pb":120.92},{"week":9,"playoff":false,"a":"drjkay","b":"AlastairL","pa":119.1,"pb":154.96},{"week":9,"playoff":false,"a":"joshjr11","b":"GSac","pa":165.3,"pb":123.54},{"week":9,"playoff":false,"a":"benjlev","b":"sanfbe","pa":124.56,"pb":125.42},{"week":10,"playoff":false,"a":"dpol","b":"AlastairL","pa":127.7,"pb":148.7},{"week":10,"playoff":false,"a":"saulgoat","b":"drjkay","pa":145.5,"pb":146.6},{"week":10,"playoff":false,"a":"joshjr11","b":"sanfbe","pa":162.8,"pb":103.44},{"week":10,"playoff":false,"a":"benjlev","b":"GSac","pa":99.74,"pb":157.52},{"week":11,"playoff":false,"a":"joshjr11","b":"dpol","pa":114.54,"pb":116.44},{"week":11,"playoff":false,"a":"benjlev","b":"drjkay","pa":98.02,"pb":131.72},{"week":11,"playoff":false,"a":"saulgoat","b":"GSac","pa":128.96,"pb":67.9},{"week":11,"playoff":false,"a":"AlastairL","b":"sanfbe","pa":140.74,"pb":160.48},{"week":12,"playoff":false,"a":"benjlev","b":"dpol","pa":96.02,"pb":121.48},{"week":12,"playoff":false,"a":"joshjr11","b":"drjkay","pa":113.46,"pb":130.08},{"week":12,"playoff":false,"a":"saulgoat","b":"sanfbe","pa":105.12,"pb":95.82},{"week":12,"playoff":false,"a":"AlastairL","b":"GSac","pa":133.56,"pb":202.36},{"week":13,"playoff":false,"a":"dpol","b":"GSac","pa":157.44,"pb":100.2},{"week":13,"playoff":false,"a":"drjkay","b":"sanfbe","pa":141,"pb":88.52},{"week":13,"playoff":false,"a":"joshjr11","b":"saulgoat","pa":148.64,"pb":114.16},{"week":13,"playoff":false,"a":"benjlev","b":"AlastairL","pa":110.14,"pb":95.02},{"week":14,"playoff":false,"a":"dpol","b":"sanfbe","pa":146.4,"pb":155.04},{"week":14,"playoff":false,"a":"drjkay","b":"GSac","pa":146.54,"pb":107.9},{"week":14,"playoff":false,"a":"benjlev","b":"saulgoat","pa":102.66,"pb":136.46},{"week":14,"playoff":false,"a":"joshjr11","b":"AlastairL","pa":113.86,"pb":130.84},{"week":15,"playoff":true,"a":"joshjr11","b":"saulgoat","pa":143.2,"pb":172.42},{"week":15,"playoff":true,"a":"drjkay","b":"sanfbe","pa":94.94,"pb":160.82},{"week":15,"playoff":true,"a":"benjlev","b":"GSac","pa":98.9,"pb":107.4},{"week":16,"playoff":true,"a":"dpol","b":"sanfbe","pa":161.02,"pb":108.9},{"week":16,"playoff":true,"a":"saulgoat","b":"AlastairL","pa":113.46,"pb":130.5},{"week":16,"playoff":true,"a":"joshjr11","b":"drjkay","pa":173.36,"pb":116.02},{"week":17,"playoff":true,"a":"dpol","b":"AlastairL","pa":118.58,"pb":143.96},{"week":17,"playoff":true,"a":"saulgoat","b":"sanfbe","pa":76.78,"pb":143.58}]},{"season":"2026","name":"Borehamwood","champion":null,"standings":[{"manager":"joshjr11","team":"Denver Brochos","wins":0,"losses":0,"pf":0,"pa":0,"high":0},{"manager":"benjlev","team":"benjlev","wins":0,"losses":0,"pf":0,"pa":0,"high":0},{"manager":"dpol","team":"Plancey Neutral","wins":0,"losses":0,"pf":0,"pa":0,"high":0},{"manager":"saulgoat","team":"Love Thy Naber","wins":0,"losses":0,"pf":0,"pa":0,"high":0},{"manager":"drjkay","team":"A rookie error","wins":0,"losses":0,"pf":0,"pa":0,"high":0},{"manager":"AlastairL","team":"Fourth and Goalda Meir","wins":0,"losses":0,"pf":0,"pa":0,"high":0},{"manager":"GSac","team":"Jewish Mccaffrey","wins":0,"losses":0,"pf":0,"pa":0,"high":0},{"manager":"sanfbe","team":"J’Allen Plancey z’l","wins":0,"losses":0,"pf":0,"pa":0,"high":0}],"games":[]}],"meta":{"generated":"2026-07-21T13:08:07.708Z"}};
const STATS_DATA = {"league":"Borehamwood","season":"2025","standings":[{"manager":"dpol","team":"Plancey Neutral","wins":11,"losses":3,"ties":0,"pf":1878.92,"pa":1696.02,"streak":0},{"manager":"AlastairL","team":"Fourth and Goalda Meir","wins":9,"losses":5,"ties":0,"pf":1828.36,"pa":1785.46,"streak":0},{"manager":"sanfbe","team":"J’Allen Plancey z’l","wins":8,"losses":6,"ties":0,"pf":1778.06,"pa":1854.68,"streak":0},{"manager":"saulgoat","team":"Love Thy Naber","wins":8,"losses":6,"ties":0,"pf":1689.66,"pa":1588.06,"streak":0},{"manager":"joshjr11","team":"Denver Brochos","wins":6,"losses":8,"ties":0,"pf":1818.96,"pa":1792.34,"streak":0},{"manager":"drjkay","team":"A rookie error","wins":6,"losses":8,"ties":0,"pf":1770.74,"pa":1824.38,"streak":0},{"manager":"GSac","team":"This One Really Hurts","wins":5,"losses":9,"ties":0,"pf":1786.8,"pa":1833.52,"streak":0},{"manager":"benjlev","team":"benjlev","wins":3,"losses":11,"ties":0,"pf":1562.56,"pa":1739.6,"streak":0}],"headToHead":{"dpol":{"drjkay":{"wins":2,"losses":0,"pf":254.18,"pa":218.26},"saulgoat":{"wins":1,"losses":1,"pf":217.98,"pa":258.52},"AlastairL":{"wins":1,"losses":1,"pf":259.56,"pa":280.54},"joshjr11":{"wins":2,"losses":0,"pf":270.64,"pa":256.42},"benjlev":{"wins":2,"losses":0,"pf":257.9,"pa":231.18},"GSac":{"wins":2,"losses":0,"pf":285.82,"pa":218.62},"sanfbe":{"wins":1,"losses":1,"pf":332.84,"pa":232.48}},"drjkay":{"dpol":{"wins":0,"losses":2,"pf":218.26,"pa":254.18},"AlastairL":{"wins":0,"losses":2,"pf":245.18,"pa":288.84},"saulgoat":{"wins":1,"losses":1,"pf":260,"pa":283.12},"benjlev":{"wins":2,"losses":0,"pf":300.74,"pa":187},"joshjr11":{"wins":1,"losses":1,"pf":232.12,"pa":259.18},"sanfbe":{"wins":1,"losses":1,"pf":249.74,"pa":252.12},"GSac":{"wins":1,"losses":1,"pf":264.7,"pa":299.94}},"saulgoat":{"AlastairL":{"wins":0,"losses":2,"pf":197.6,"pa":218.96},"dpol":{"wins":1,"losses":1,"pf":258.52,"pa":217.98},"drjkay":{"wins":1,"losses":1,"pf":283.12,"pa":260},"GSac":{"wins":2,"losses":0,"pf":260.24,"pa":189.1},"sanfbe":{"wins":2,"losses":0,"pf":245.66,"pa":234.24},"joshjr11":{"wins":0,"losses":2,"pf":221.12,"pa":285.8},"benjlev":{"wins":2,"losses":0,"pf":223.4,"pa":181.98}},"AlastairL":{"saulgoat":{"wins":2,"losses":0,"pf":218.96,"pa":197.6},"drjkay":{"wins":2,"losses":0,"pf":288.84,"pa":245.18},"dpol":{"wins":1,"losses":1,"pf":280.54,"pa":259.56},"sanfbe":{"wins":0,"losses":2,"pf":281.26,"pa":311.74},"GSac":{"wins":1,"losses":1,"pf":261.28,"pa":323.86},"benjlev":{"wins":1,"losses":1,"pf":210.06,"pa":213.68},"joshjr11":{"wins":2,"losses":0,"pf":287.42,"pa":233.84}},"joshjr11":{"benjlev":{"wins":0,"losses":2,"pf":237.8,"pa":317.6},"GSac":{"wins":2,"losses":0,"pf":292.94,"pa":224.98},"sanfbe":{"wins":1,"losses":1,"pf":252.98,"pa":238.46},"dpol":{"wins":0,"losses":2,"pf":256.42,"pa":270.64},"drjkay":{"wins":1,"losses":1,"pf":259.18,"pa":232.12},"saulgoat":{"wins":2,"losses":0,"pf":285.8,"pa":221.12},"AlastairL":{"wins":0,"losses":2,"pf":233.84,"pa":287.42}},"benjlev":{"joshjr11":{"wins":2,"losses":0,"pf":317.6,"pa":237.8},"sanfbe":{"wins":0,"losses":2,"pf":234.46,"pa":237.14},"GSac":{"wins":0,"losses":2,"pf":196.66,"pa":272.56},"drjkay":{"wins":0,"losses":2,"pf":187,"pa":300.74},"dpol":{"wins":0,"losses":2,"pf":231.18,"pa":257.9},"AlastairL":{"wins":1,"losses":1,"pf":213.68,"pa":210.06},"saulgoat":{"wins":0,"losses":2,"pf":181.98,"pa":223.4}},"GSac":{"sanfbe":{"wins":1,"losses":1,"pf":257.74,"pa":271.88},"joshjr11":{"wins":0,"losses":2,"pf":224.98,"pa":292.94},"benjlev":{"wins":2,"losses":0,"pf":272.56,"pa":196.66},"saulgoat":{"wins":0,"losses":2,"pf":189.1,"pa":260.24},"AlastairL":{"wins":1,"losses":1,"pf":323.86,"pa":261.28},"dpol":{"wins":0,"losses":2,"pf":218.62,"pa":285.82},"drjkay":{"wins":1,"losses":1,"pf":299.94,"pa":264.7}},"sanfbe":{"GSac":{"wins":1,"losses":1,"pf":271.88,"pa":257.74},"benjlev":{"wins":2,"losses":0,"pf":237.14,"pa":234.46},"joshjr11":{"wins":1,"losses":1,"pf":238.46,"pa":252.98},"AlastairL":{"wins":2,"losses":0,"pf":311.74,"pa":281.26},"saulgoat":{"wins":0,"losses":2,"pf":234.24,"pa":245.66},"drjkay":{"wins":1,"losses":1,"pf":252.12,"pa":249.74},"dpol":{"wins":1,"losses":1,"pf":232.48,"pa":332.84}}},"extremes":{"closestGame":{"week":3,"playoff":false,"a":"dpol","b":"AlastairL","pa":131.86,"pb":131.84,"ap":{"QB":15.16,"RB":50,"WR":33.5,"TE":14.7,"K":9.5,"DEF":9},"bp":{"QB":14.04,"RB":54.7,"WR":43.7,"TE":7.8,"K":8.6,"DEF":3},"as":[{"n":"Patrick Mahomes","pos":"QB","pts":15.16},{"n":"Jahmyr Gibbs","pos":"RB","pts":24.4},{"n":"James Cook","pos":"RB","pts":19.3},{"n":"Zay Flowers","pos":"WR","pts":2.3},{"n":"Jaxon Smith-Njigba","pos":"WR","pts":18.1},{"n":"Davante Adams","pos":"WR","pts":13.1},{"n":"Jake Ferguson","pos":"TE","pts":14.7},{"n":"Ashton Jeanty","pos":"RB","pts":6.3},{"n":"Jake Bates","pos":"K","pts":9.5},{"n":"GB","pos":"DEF","pts":9}],"bs":[{"n":"Dak Prescott","pos":"QB","pts":14.04},{"n":"Christian McCaffrey","pos":"RB","pts":19},{"n":"Kyren Williams","pos":"RB","pts":18.2},{"n":"Amon-Ra St. Brown","pos":"WR","pts":17.2},{"n":"DJ Moore","pos":"WR","pts":10.8},{"n":"Ricky Pearsall","pos":"WR","pts":15.7},{"n":"Brock Bowers","pos":"TE","pts":7.8},{"n":"Kenneth Walker","pos":"RB","pts":17.5},{"n":"Brandon Aubrey","pos":"K","pts":8.6},{"n":"ATL","pos":"DEF","pts":3}],"ab":[{"n":"Cam Skattebo","pos":"RB","pts":21.1},{"n":"A.J. Brown","pos":"WR","pts":19.9},{"n":"Matthew Golden","pos":"WR","pts":8.1},{"n":"Marquise Brown","pos":"WR","pts":6.2},{"n":"Jerry Jeudy","pos":"WR","pts":2.2},{"n":"Jauan Jennings","pos":"WR","pts":0}],"bb":[{"n":"Drake Maye","pos":"QB","pts":23.22},{"n":"D'Andre Swift","pos":"RB","pts":9.3},{"n":"Jameson Williams","pos":"WR","pts":5.3},{"n":"Brian Robinson","pos":"RB","pts":2.2},{"n":"Xavier Worthy","pos":"WR","pts":0},{"n":"Justin Fields","pos":"QB","pts":0}]},"biggestBlowout":{"week":7,"playoff":false,"a":"dpol","b":"sanfbe","pa":186.44,"pb":77.44,"ap":{"QB":32.24,"RB":52.2,"WR":77.9,"TE":14.3,"K":8.8,"DEF":1},"bp":{"QB":12.34,"RB":46.6,"WR":6.5,"TE":3,"K":2,"DEF":7},"as":[{"n":"Patrick Mahomes","pos":"QB","pts":32.24},{"n":"Jahmyr Gibbs","pos":"RB","pts":35.3},{"n":"Cam Skattebo","pos":"RB","pts":16.9},{"n":"A.J. Brown","pos":"WR","pts":26.1},{"n":"Jaxon Smith-Njigba","pos":"WR","pts":22.3},{"n":"CeeDee Lamb","pos":"WR","pts":19.5},{"n":"Tucker Kraft","pos":"TE","pts":14.3},{"n":"Quentin Johnston","pos":"WR","pts":10},{"n":"Jake Bates","pos":"K","pts":8.8},{"n":"PIT","pos":"DEF","pts":1}],"bs":[{"n":"Jared Goff","pos":"QB","pts":12.34},{"n":"Josh Jacobs","pos":"RB","pts":18.3},{"n":"Rico Dowdle","pos":"RB","pts":10.1},{"n":"Mike Evans","pos":"WR","pts":0},{"n":"DK Metcalf","pos":"WR","pts":6.5},{"n":"Luke McCaffrey","pos":"WR","pts":0},{"n":"Sam LaPorta","pos":"TE","pts":3},{"n":"Bijan Robinson","pos":"RB","pts":18.2},{"n":"Matt Gay","pos":"K","pts":2},{"n":"IND","pos":"DEF","pts":7}],"ab":[{"n":"Isiah Pacheco","pos":"RB","pts":11.9},{"n":"Matthew Golden","pos":"WR","pts":5.7},{"n":"Jauan Jennings","pos":"WR","pts":5.1},{"n":"Alvin Kamara","pos":"RB","pts":4.4},{"n":"James Cook","pos":"RB","pts":0},{"n":"Zay Flowers","pos":"WR","pts":0}],"bb":[{"n":"Zach Charbonnet","pos":"RB","pts":18.5},{"n":"Jaylen Warren","pos":"RB","pts":17.8},{"n":"Chuba Hubbard","pos":"RB","pts":6.5},{"n":"Cedric Tillman","pos":"WR","pts":0},{"n":"Josh Allen","pos":"QB","pts":0},{"n":"Deebo Samuel","pos":"WR","pts":0},{"n":"Garrett Wilson","pos":"WR","pts":0}]},"highestScoringGame":{"week":12,"playoff":false,"a":"AlastairL","b":"GSac","pa":133.56,"pb":202.36,"ap":{"QB":17.96,"RB":54.5,"WR":28.4,"TE":8.5,"K":7.2,"DEF":17},"bp":{"QB":32.86,"RB":91.1,"WR":39.4,"TE":8.6,"K":13.4,"DEF":17},"as":[{"n":"Drake Maye","pos":"QB","pts":17.96},{"n":"Kyren Williams","pos":"RB","pts":5.3},{"n":"Emanuel Wilson","pos":"RB","pts":25.5},{"n":"Amon-Ra St. Brown","pos":"WR","pts":25.4},{"n":"Jameson Williams","pos":"WR","pts":0},{"n":"Stefon Diggs","pos":"WR","pts":3},{"n":"Brock Bowers","pos":"TE","pts":8.5},{"n":"Christian McCaffrey","pos":"RB","pts":23.7},{"n":"Brandon Aubrey","pos":"K","pts":7.2},{"n":"CLE","pos":"DEF","pts":17}],"bs":[{"n":"Jalen Hurts","pos":"QB","pts":32.86},{"n":"Jahmyr Gibbs","pos":"RB","pts":49.9},{"n":"James Cook","pos":"RB","pts":20.4},{"n":"DeVonta Smith","pos":"WR","pts":11.9},{"n":"Davante Adams","pos":"WR","pts":20.7},{"n":"Rome Odunze","pos":"WR","pts":6.8},{"n":"Jake Ferguson","pos":"TE","pts":8.6},{"n":"Ashton Jeanty","pos":"RB","pts":20.8},{"n":"Cam Little","pos":"K","pts":13.4},{"n":"ATL","pos":"DEF","pts":17}],"ab":[{"n":"Matthew Stafford","pos":"QB","pts":28.92},{"n":"Khalil Shakir","pos":"WR","pts":13},{"n":"Kenneth Walker","pos":"RB","pts":11.6},{"n":"Brian Robinson","pos":"RB","pts":4.9},{"n":"Ricky Pearsall","pos":"WR","pts":1.8},{"n":"D'Andre Swift","pos":"RB","pts":1.4}],"bb":[{"n":"Wan'Dale Robinson","pos":"WR","pts":26.1},{"n":"Jauan Jennings","pos":"WR","pts":12.6},{"n":"Tyjae Spears","pos":"RB","pts":4.5},{"n":"Jerry Jeudy","pos":"WR","pts":2.4},{"n":"Chris Godwin","pos":"WR","pts":1.9},{"n":"Rashid Shaheed","pos":"WR","pts":0.5}]}},"meta":{"generated":"2026-07-21T13:08:11.383Z"}};
const ROSTERS_DATA = {"Denver Brochos (@joshjr11)":[{"id":"10229","name":"Rashee Rice","pos":"WR"},{"id":"11631","name":"Brian Thomas","pos":"WR"},{"id":"12474","name":"Woody Marks","pos":"RB"},{"id":"3451","name":"Ka'imi Fairbairn","pos":"K"},{"id":"4137","name":"James Conner","pos":"RB"},{"id":"6770","name":"Joe Burrow","pos":"QB"},{"id":"6794","name":"Justin Jefferson","pos":"WR"},{"id":"7564","name":"Ja'Marr Chase","pos":"WR"},{"id":"7594","name":"Chuba Hubbard","pos":"RB"},{"id":"8112","name":"Drake London","pos":"WR"},{"id":"8130","name":"Trey McBride","pos":"TE"},{"id":"8144","name":"Chris Olave","pos":"WR"},{"id":"8183","name":"Brock Purdy","pos":"QB"},{"id":"9224","name":"Chase Brown","pos":"RB"},{"id":"9226","name":"De'Von Achane","pos":"RB"},{"id":"CHI","name":"Chicago Bears","pos":"DEF"},{"id":"DEN","name":"Denver Broncos","pos":"DEF"}],"benjlev (@benjlev)":[{"id":"10213","name":"Tre Tucker","pos":"WR"},{"id":"11635","name":"Ladd McConkey","pos":"WR"},{"id":"11638","name":"Ricky Pearsall","pos":"WR"},{"id":"1466","name":"Travis Kelce","pos":"TE"},{"id":"2747","name":"Jason Myers","pos":"K"},{"id":"3198","name":"Derrick Henry","pos":"RB"},{"id":"3321","name":"Tyreek Hill","pos":"WR"},{"id":"4199","name":"Aaron Jones","pos":"RB"},{"id":"4866","name":"Saquon Barkley","pos":"RB"},{"id":"4881","name":"Lamar Jackson","pos":"QB"},{"id":"4983","name":"DJ Moore","pos":"WR"},{"id":"7523","name":"Trevor Lawrence","pos":"QB"},{"id":"7611","name":"Rhamondre Stevenson","pos":"RB"},{"id":"8142","name":"Alec Pierce","pos":"WR"},{"id":"8155","name":"Breece Hall","pos":"RB"},{"id":"9756","name":"Jordan Addison","pos":"WR"},{"id":"NYG","name":"New York Giants","pos":"DEF"}],"Plancey Neutral (@dpol)":[{"id":"11584","name":"Bucky Irving","pos":"RB"},{"id":"11655","name":"Tyrone Tracy","pos":"RB"},{"id":"12481","name":"Cam Skattebo","pos":"RB"},{"id":"12507","name":"Omarion Hampton","pos":"RB"},{"id":"2216","name":"Mike Evans","pos":"WR"},{"id":"3214","name":"Hunter Henry","pos":"TE"},{"id":"3257","name":"Jacoby Brissett","pos":"QB"},{"id":"4217","name":"George Kittle","pos":"TE"},{"id":"4381","name":"Taysom Hill","pos":"TE"},{"id":"5859","name":"A.J. Brown","pos":"WR"},{"id":"5947","name":"Jakobi Meyers","pos":"WR"},{"id":"6786","name":"CeeDee Lamb","pos":"WR"},{"id":"6813","name":"Jonathan Taylor","pos":"RB"},{"id":"7839","name":"Evan McPherson","pos":"K"},{"id":"8126","name":"Wan'Dale Robinson","pos":"WR"},{"id":"9493","name":"Puka Nacua","pos":"WR"},{"id":"NE","name":"New England Patriots","pos":"DEF"}],"Love Thy Naber (@saulgoat)":[{"id":"11539","name":"Jake Bates","pos":"K"},{"id":"11586","name":"Blake Corum","pos":"RB"},{"id":"11589","name":"Trey Benson","pos":"RB"},{"id":"11632","name":"Malik Nabers","pos":"WR"},{"id":"11643","name":"Jaylen Wright","pos":"RB"},{"id":"12506","name":"Harold Fannin","pos":"TE"},{"id":"12529","name":"TreVeyon Henderson","pos":"RB"},{"id":"2505","name":"Darren Waller","pos":"TE"},{"id":"3163","name":"Jared Goff","pos":"QB"},{"id":"5045","name":"Courtland Sutton","pos":"WR"},{"id":"6801","name":"Tee Higgins","pos":"WR"},{"id":"7526","name":"Jaylen Waddle","pos":"WR"},{"id":"7543","name":"Travis Etienne","pos":"RB"},{"id":"7569","name":"Nico Collins","pos":"WR"},{"id":"7588","name":"Javonte Williams","pos":"RB"},{"id":"7607","name":"Michael Carter","pos":"RB"},{"id":"8408","name":"Jordan Mason","pos":"RB"},{"id":"BUF","name":"Buffalo Bills","pos":"DEF"}],"A rookie error (@drjkay)":[{"id":"10236","name":"Dalton Kincaid","pos":"TE"},{"id":"12489","name":"RJ Harvey","pos":"RB"},{"id":"12508","name":"Jaxson Dart","pos":"QB"},{"id":"12512","name":"Quinshon Judkins","pos":"RB"},{"id":"12514","name":"Emeka Egbuka","pos":"WR"},{"id":"12526","name":"Tetairoa McMillan","pos":"WR"},{"id":"12711","name":"Tyler Loop","pos":"K"},{"id":"3294","name":"Dak Prescott","pos":"QB"},{"id":"4098","name":"Kareem Hunt","pos":"RB"},{"id":"6806","name":"J.K. Dobbins","pos":"RB"},{"id":"6819","name":"Michael Pittman","pos":"WR"},{"id":"7567","name":"Kenny Gainwell","pos":"RB"},{"id":"8137","name":"George Pickens","pos":"WR"},{"id":"8228","name":"Jaylen Warren","pos":"RB"},{"id":"9480","name":"Brenton Strange","pos":"TE"},{"id":"9488","name":"Jaxon Smith-Njigba","pos":"WR"},{"id":"SEA","name":"Seattle Seahawks","pos":"DEF"}],"Fourth and Goalda Meir (@AlastairL)":[{"id":"11533","name":"Brandon Aubrey","pos":"K"},{"id":"11564","name":"Drake Maye","pos":"QB"},{"id":"11604","name":"Brock Bowers","pos":"TE"},{"id":"12519","name":"Luther Burden","pos":"WR"},{"id":"2449","name":"Stefon Diggs","pos":"WR"},{"id":"4034","name":"Christian McCaffrey","pos":"RB"},{"id":"421","name":"Matthew Stafford","pos":"QB"},{"id":"6790","name":"D'Andre Swift","pos":"RB"},{"id":"7002","name":"Juwan Johnson","pos":"TE"},{"id":"7547","name":"Amon-Ra St. Brown","pos":"WR"},{"id":"7553","name":"Kyle Pitts","pos":"TE"},{"id":"8132","name":"Tyler Allgeier","pos":"RB"},{"id":"8148","name":"Jameson Williams","pos":"WR"},{"id":"8150","name":"Kyren Williams","pos":"RB"},{"id":"8151","name":"Kenneth Walker","pos":"RB"},{"id":"8154","name":"Brian Robinson","pos":"RB"},{"id":"PIT","name":"Pittsburgh Steelers","pos":"DEF"}],"This One Really Hurts (@GSac)":[{"id":"11579","name":"Audric Estime","pos":"RB"},{"id":"11620","name":"Rome Odunze","pos":"WR"},{"id":"11786","name":"Cam Little","pos":"K"},{"id":"12476","name":"Devin Neal","pos":"RB"},{"id":"12527","name":"Ashton Jeanty","pos":"RB"},{"id":"12530","name":"Travis Hunter","pos":null},{"id":"2133","name":"Davante Adams","pos":"WR"},{"id":"6904","name":"Jalen Hurts","pos":"QB"},{"id":"7049","name":"Jauan Jennings","pos":"WR"},{"id":"7525","name":"DeVonta Smith","pos":"WR"},{"id":"8110","name":"Jake Ferguson","pos":"TE"},{"id":"8134","name":"Khalil Shakir","pos":"WR"},{"id":"8138","name":"James Cook","pos":"RB"},{"id":"8167","name":"Christian Watson","pos":"WR"},{"id":"9221","name":"Jahmyr Gibbs","pos":"RB"},{"id":"9504","name":"Kayshon Boutte","pos":"WR"},{"id":"9508","name":"Tyjae Spears","pos":"RB"},{"id":"JAX","name":"Jacksonville Jaguars","pos":"DEF"}],"J’Allen Plancey z’l (@sanfbe)":[{"id":"10222","name":"Jayden Reed","pos":"WR"},{"id":"10232","name":"Michael Wilson","pos":"WR"},{"id":"10859","name":"Sam LaPorta","pos":"TE"},{"id":"11627","name":"Troy Franklin","pos":"WR"},{"id":"12534","name":"Kyle Monangai","pos":"RB"},{"id":"4984","name":"Josh Allen","pos":"QB"},{"id":"5022","name":"Dallas Goedert","pos":"TE"},{"id":"5189","name":"Eddy Pineiro","pos":"K"},{"id":"5846","name":"DK Metcalf","pos":"WR"},{"id":"5850","name":"Josh Jacobs","pos":"RB"},{"id":"5872","name":"Deebo Samuel","pos":"WR"},{"id":"6865","name":"Colby Parkinson","pos":"TE"},{"id":"7021","name":"Rico Dowdle","pos":"RB"},{"id":"8146","name":"Garrett Wilson","pos":"WR"},{"id":"9509","name":"Bijan Robinson","pos":"RB"},{"id":"9753","name":"Zach Charbonnet","pos":"RB"},{"id":"9997","name":"Zay Flowers","pos":"WR"},{"id":"TB","name":"Tampa Bay Buccaneers","pos":"DEF"}]};
const TRADE_VALUES = [{"player":{"name":"Bijan Robinson","sleeperId":"9509","position":"RB"},"redraftValue":10493},{"player":{"name":"Jahmyr Gibbs","sleeperId":"9221","position":"RB"},"redraftValue":10324},{"player":{"name":"Ja'Marr Chase","sleeperId":"7564","position":"WR"},"redraftValue":10038},{"player":{"name":"Puka Nacua","sleeperId":"9493","position":"WR"},"redraftValue":8920},{"player":{"name":"Jonathan Taylor","sleeperId":"6813","position":"RB"},"redraftValue":8568},{"player":{"name":"Jaxon Smith-Njigba","sleeperId":"9488","position":"WR"},"redraftValue":8697},{"player":{"name":"Justin Jefferson","sleeperId":"6794","position":"WR"},"redraftValue":8544},{"player":{"name":"Amon-Ra St. Brown","sleeperId":"7547","position":"WR"},"redraftValue":8446},{"player":{"name":"Ashton Jeanty","sleeperId":"12527","position":"RB"},"redraftValue":8114},{"player":{"name":"Christian McCaffrey","sleeperId":"4034","position":"RB"},"redraftValue":8019},{"player":{"name":"James Cook","sleeperId":"8138","position":"RB"},"redraftValue":7987},{"player":{"name":"De'Von Achane","sleeperId":"9226","position":"RB"},"redraftValue":7901},{"player":{"name":"Omarion Hampton","sleeperId":"12507","position":"RB"},"redraftValue":7456},{"player":{"name":"CeeDee Lamb","sleeperId":"6786","position":"WR"},"redraftValue":7643},{"player":{"name":"Saquon Barkley","sleeperId":"4866","position":"RB"},"redraftValue":7032},{"player":{"name":"Jeremiyah Love","sleeperId":"13287","position":"RB"},"redraftValue":7018},{"player":{"name":"Trey McBride","sleeperId":"8130","position":"TE"},"redraftValue":6604},{"player":{"name":"Kenneth Walker","sleeperId":"8151","position":"RB"},"redraftValue":6362},{"player":{"name":"Derrick Henry","sleeperId":"3198","position":"RB"},"redraftValue":6253},{"player":{"name":"Brock Bowers","sleeperId":"11604","position":"TE"},"redraftValue":6262},{"player":{"name":"Chase Brown","sleeperId":"9224","position":"RB"},"redraftValue":5898},{"player":{"name":"Drake London","sleeperId":"8112","position":"WR"},"redraftValue":5943},{"player":{"name":"Josh Allen","sleeperId":"4984","position":"QB"},"redraftValue":6202},{"player":{"name":"Malik Nabers","sleeperId":"11632","position":"WR"},"redraftValue":5880},{"player":{"name":"Breece Hall","sleeperId":"8155","position":"RB"},"redraftValue":5390},{"player":{"name":"Nico Collins","sleeperId":"7569","position":"WR"},"redraftValue":5165},{"player":{"name":"A.J. Brown","sleeperId":"5859","position":"WR"},"redraftValue":5155},{"player":{"name":"Kyren Williams","sleeperId":"8150","position":"RB"},"redraftValue":4702},{"player":{"name":"George Pickens","sleeperId":"8137","position":"WR"},"redraftValue":4809},{"player":{"name":"Travis Etienne","sleeperId":"7543","position":"RB"},"redraftValue":4575},{"player":{"name":"TreVeyon Henderson","sleeperId":"12529","position":"RB"},"redraftValue":4515},{"player":{"name":"Josh Jacobs","sleeperId":"5850","position":"RB"},"redraftValue":4472},{"player":{"name":"Garrett Wilson","sleeperId":"8146","position":"WR"},"redraftValue":4218},{"player":{"name":"Tetairoa McMillan","sleeperId":"12526","position":"WR"},"redraftValue":4191},{"player":{"name":"Javonte Williams","sleeperId":"7588","position":"RB"},"redraftValue":3955},{"player":{"name":"Quinshon Judkins","sleeperId":"12512","position":"RB"},"redraftValue":3875},{"player":{"name":"Emeka Egbuka","sleeperId":"12514","position":"WR"},"redraftValue":3826},{"player":{"name":"Chris Olave","sleeperId":"8144","position":"WR"},"redraftValue":3785},{"player":{"name":"Cam Skattebo","sleeperId":"12481","position":"RB"},"redraftValue":3678},{"player":{"name":"DeVonta Smith","sleeperId":"7525","position":"WR"},"redraftValue":3696},{"player":{"name":"David Montgomery","sleeperId":"5892","position":"RB"},"redraftValue":3595},{"player":{"name":"Jaylen Waddle","sleeperId":"7526","position":"WR"},"redraftValue":3575},{"player":{"name":"Bucky Irving","sleeperId":"11584","position":"RB"},"redraftValue":3441},{"player":{"name":"Rashee Rice","sleeperId":"10229","position":"WR"},"redraftValue":3525},{"player":{"name":"Lamar Jackson","sleeperId":"4881","position":"QB"},"redraftValue":3660},{"player":{"name":"Tee Higgins","sleeperId":"6801","position":"WR"},"redraftValue":3357},{"player":{"name":"Ladd McConkey","sleeperId":"11635","position":"WR"},"redraftValue":3332},{"player":{"name":"Drake Maye","sleeperId":"11564","position":"QB"},"redraftValue":3417},{"player":{"name":"Zay Flowers","sleeperId":"9997","position":"WR"},"redraftValue":3206},{"player":{"name":"Tyler Warren","sleeperId":"12518","position":"TE"},"redraftValue":3133},{"player":{"name":"D'Andre Swift","sleeperId":"6790","position":"RB"},"redraftValue":2990},{"player":{"name":"Joe Burrow","sleeperId":"6770","position":"QB"},"redraftValue":3130},{"player":{"name":"Colston Loveland","sleeperId":"12517","position":"TE"},"redraftValue":2921},{"player":{"name":"Davante Adams","sleeperId":"2133","position":"WR"},"redraftValue":2865},{"player":{"name":"Jayden Daniels","sleeperId":"11566","position":"QB"},"redraftValue":2938},{"player":{"name":"Jadarian Price","sleeperId":"13286","position":"RB"},"redraftValue":2604},{"player":{"name":"Carnell Tate","sleeperId":"13279","position":"WR"},"redraftValue":2624},{"player":{"name":"Patrick Mahomes","sleeperId":"4046","position":"QB"},"redraftValue":2734},{"player":{"name":"Caleb Williams","sleeperId":"11560","position":"QB"},"redraftValue":2517},{"player":{"name":"Justin Herbert","sleeperId":"6797","position":"QB"},"redraftValue":2396},{"player":{"name":"Terry McLaurin","sleeperId":"5927","position":"WR"},"redraftValue":2175},{"player":{"name":"DJ Moore","sleeperId":"4983","position":"WR"},"redraftValue":2127},{"player":{"name":"Rome Odunze","sleeperId":"11620","position":"WR"},"redraftValue":2124},{"player":{"name":"Marvin Harrison Jr","sleeperId":"11628","position":"WR"},"redraftValue":2118},{"player":{"name":"Mike Evans","sleeperId":"2216","position":"WR"},"redraftValue":2101},{"player":{"name":"Tucker Kraft","sleeperId":"9484","position":"TE"},"redraftValue":1977},{"player":{"name":"Brian Thomas","sleeperId":"11631","position":"WR"},"redraftValue":1878},{"player":{"name":"George Kittle","sleeperId":"4217","position":"TE"},"redraftValue":1897},{"player":{"name":"Jaylen Warren","sleeperId":"8228","position":"RB"},"redraftValue":1824},{"player":{"name":"Luther Burden","sleeperId":"12519","position":"WR"},"redraftValue":1857},{"player":{"name":"Jameson Williams","sleeperId":"8148","position":"WR"},"redraftValue":1779},{"player":{"name":"Jordyn Tyson","sleeperId":"13281","position":"WR"},"redraftValue":1747},{"player":{"name":"Jalen Hurts","sleeperId":"6904","position":"QB"},"redraftValue":1779},{"player":{"name":"Tony Pollard","sleeperId":"5967","position":"RB"},"redraftValue":1656},{"player":{"name":"Chuba Hubbard","sleeperId":"7594","position":"RB"},"redraftValue":1612},{"player":{"name":"Christian Watson","sleeperId":"8167","position":"WR"},"redraftValue":1652},{"player":{"name":"Rico Dowdle","sleeperId":"7021","position":"RB"},"redraftValue":1573},{"player":{"name":"Rhamondre Stevenson","sleeperId":"7611","position":"RB"},"redraftValue":1519},{"player":{"name":"Bhayshul Tuten","sleeperId":"12490","position":"RB"},"redraftValue":1508},{"player":{"name":"Sam LaPorta","sleeperId":"10859","position":"TE"},"redraftValue":1514},{"player":{"name":"RJ Harvey","sleeperId":"12489","position":"RB"},"redraftValue":1448},{"player":{"name":"Travis Kelce","sleeperId":"1466","position":"TE"},"redraftValue":1486},{"player":{"name":"Kyle Pitts","sleeperId":"7553","position":"TE"},"redraftValue":1474},{"player":{"name":"Makai Lemon","sleeperId":"13294","position":"WR"},"redraftValue":1422},{"player":{"name":"DK Metcalf","sleeperId":"5846","position":"WR"},"redraftValue":1363},{"player":{"name":"Rachaad White","sleeperId":"8136","position":"RB"},"redraftValue":1304},{"player":{"name":"Dak Prescott","sleeperId":"3294","position":"QB"},"redraftValue":1367},{"player":{"name":"J.K. Dobbins","sleeperId":"6806","position":"RB"},"redraftValue":1223},{"player":{"name":"Trevor Lawrence","sleeperId":"7523","position":"QB"},"redraftValue":1307},{"player":{"name":"Aaron Jones","sleeperId":"4199","position":"RB"},"redraftValue":1203},{"player":{"name":"Alec Pierce","sleeperId":"8142","position":"WR"},"redraftValue":1131},{"player":{"name":"Blake Corum","sleeperId":"11586","position":"RB"},"redraftValue":1091},{"player":{"name":"Kyle Monangai","sleeperId":"12534","position":"RB"},"redraftValue":1089},{"player":{"name":"Kenneth Gainwell","sleeperId":"7567","position":"RB"},"redraftValue":1046},{"player":{"name":"Zach Charbonnet","sleeperId":"9753","position":"RB"},"redraftValue":1040},{"player":{"name":"Michael Pittman","sleeperId":"6819","position":"WR"},"redraftValue":1048},{"player":{"name":"Bo Nix","sleeperId":"11563","position":"QB"},"redraftValue":1085},{"player":{"name":"Jordan Mason","sleeperId":"8408","position":"RB"},"redraftValue":895},{"player":{"name":"Courtland Sutton","sleeperId":"5045","position":"WR"},"redraftValue":854},{"player":{"name":"Brock Purdy","sleeperId":"8183","position":"QB"},"redraftValue":876},{"player":{"name":"Jaxson Dart","sleeperId":"12508","position":"QB"},"redraftValue":875},{"player":{"name":"Alvin Kamara","sleeperId":"4035","position":"RB"},"redraftValue":797},{"player":{"name":"Harold Fannin","sleeperId":"12506","position":"TE"},"redraftValue":818},{"player":{"name":"Chris Godwin","sleeperId":"4037","position":"WR"},"redraftValue":772},{"player":{"name":"Jonathon Brooks","sleeperId":"11583","position":"RB"},"redraftValue":721},{"player":{"name":"Jordan Love","sleeperId":"6804","position":"QB"},"redraftValue":746},{"player":{"name":"Michael Wilson","sleeperId":"10232","position":"WR"},"redraftValue":707},{"player":{"name":"Matthew Stafford","sleeperId":"421","position":"QB"},"redraftValue":736},{"player":{"name":"Quentin Johnston","sleeperId":"9754","position":"WR"},"redraftValue":654},{"player":{"name":"Jakobi Meyers","sleeperId":"5947","position":"WR"},"redraftValue":654},{"player":{"name":"Jared Goff","sleeperId":"3163","position":"QB"},"redraftValue":678},{"player":{"name":"Wan'Dale Robinson","sleeperId":"8126","position":"WR"},"redraftValue":600},{"player":{"name":"Jordan Addison","sleeperId":"9756","position":"WR"},"redraftValue":599},{"player":{"name":"Stefon Diggs","sleeperId":"2449","position":"WR"},"redraftValue":567},{"player":{"name":"Jake Ferguson","sleeperId":"8110","position":"TE"},"redraftValue":560},{"player":{"name":"Ricky Pearsall","sleeperId":"11638","position":"WR"},"redraftValue":544},{"player":{"name":"Isiah Pacheco","sleeperId":"8205","position":"RB"},"redraftValue":516},{"player":{"name":"Tyler Allgeier","sleeperId":"8132","position":"RB"},"redraftValue":511},{"player":{"name":"Mark Andrews","sleeperId":"5012","position":"TE"},"redraftValue":511},{"player":{"name":"Jacory Croskey-Merritt","sleeperId":"12533","position":"RB"},"redraftValue":492},{"player":{"name":"Jonah Coleman","sleeperId":"13345","position":"RB"},"redraftValue":478},{"player":{"name":"Brian Robinson","sleeperId":"8154","position":"RB"},"redraftValue":471},{"player":{"name":"KC Concepcion","sleeperId":"13298","position":"WR"},"redraftValue":470},{"player":{"name":"Baker Mayfield","sleeperId":"4892","position":"QB"},"redraftValue":488},{"player":{"name":"Tyrone Tracy","sleeperId":"11655","position":"RB"},"redraftValue":453},{"player":{"name":"Isaiah Likely","sleeperId":"8131","position":"TE"},"redraftValue":452},{"player":{"name":"Josh Downs","sleeperId":"9500","position":"WR"},"redraftValue":444},{"player":{"name":"Chris Rodriguez","sleeperId":"10219","position":"RB"},"redraftValue":417},{"player":{"name":"Parker Washington","sleeperId":"9487","position":"WR"},"redraftValue":394},{"player":{"name":"Nicholas Singleton","sleeperId":"13288","position":"RB"},"redraftValue":351},{"player":{"name":"Dallas Goedert","sleeperId":"5022","position":"TE"},"redraftValue":342},{"player":{"name":"Travis Hunter","sleeperId":"12530","position":"WR"},"redraftValue":335},{"player":{"name":"Dalton Kincaid","sleeperId":"10236","position":"TE"},"redraftValue":334},{"player":{"name":"Jayden Reed","sleeperId":"10222","position":"WR"},"redraftValue":301},{"player":{"name":"Fernando Mendoza","sleeperId":"13269","position":"QB"},"redraftValue":293},{"player":{"name":"Woody Marks","sleeperId":"12474","position":"RB"},"redraftValue":273},{"player":{"name":"Cam Ward","sleeperId":"12522","position":"QB"},"redraftValue":291},{"player":{"name":"Rashid Shaheed","sleeperId":"8676","position":"WR"},"redraftValue":270},{"player":{"name":"Omar Cooper","sleeperId":"13276","position":"WR"},"redraftValue":265},{"player":{"name":"Tyler Shough","sleeperId":"12545","position":"QB"},"redraftValue":265},{"player":{"name":"Romeo Doubs","sleeperId":"8121","position":"WR"},"redraftValue":230},{"player":{"name":"Denzel Boston","sleeperId":"13346","position":"WR"},"redraftValue":226},{"player":{"name":"Tyreek Hill","sleeperId":"3321","position":"WR"},"redraftValue":220},{"player":{"name":"Kenyon Sadiq","sleeperId":"13330","position":"TE"},"redraftValue":220},{"player":{"name":"Chig Okonkwo","sleeperId":"8210","position":"TE"},"redraftValue":211},{"player":{"name":"Matthew Golden","sleeperId":"12501","position":"WR"},"redraftValue":205},{"player":{"name":"Jayden Higgins","sleeperId":"12484","position":"WR"},"redraftValue":204},{"player":{"name":"Keaton Mitchell","sleeperId":"9511","position":"RB"},"redraftValue":183},{"player":{"name":"Sam Darnold","sleeperId":"4943","position":"QB"},"redraftValue":195},{"player":{"name":"Eli Stowers","sleeperId":"13349","position":"TE"},"redraftValue":187},{"player":{"name":"Xavier Worthy","sleeperId":"11624","position":"WR"},"redraftValue":182},{"player":{"name":"C.J. Stroud","sleeperId":"9758","position":"QB"},"redraftValue":189},{"player":{"name":"Hunter Henry","sleeperId":"3214","position":"TE"},"redraftValue":182},{"player":{"name":"Tyjae Spears","sleeperId":"9508","position":"RB"},"redraftValue":171},{"player":{"name":"T.J. Hockenson","sleeperId":"5844","position":"TE"},"redraftValue":177},{"player":{"name":"Kyler Murray","sleeperId":"5849","position":"QB"},"redraftValue":184},{"player":{"name":"Brandon Aiyuk","sleeperId":"6803","position":"WR"},"redraftValue":159},{"player":{"name":"Juwan Johnson","sleeperId":"7002","position":"TE"},"redraftValue":153},{"player":{"name":"Tank Bigsby","sleeperId":"9225","position":"RB"},"redraftValue":142},{"player":{"name":"Brenton Strange","sleeperId":"9480","position":"TE"},"redraftValue":143},{"player":{"name":"Khalil Shakir","sleeperId":"8134","position":"WR"},"redraftValue":129},{"player":{"name":"Oronde Gadsden","sleeperId":"12493","position":"TE"},"redraftValue":121},{"player":{"name":"Jalen McMillan","sleeperId":"11618","position":"WR"},"redraftValue":118},{"player":{"name":"Jalen Coker","sleeperId":"11646","position":"WR"},"redraftValue":113},{"player":{"name":"Jauan Jennings","sleeperId":"7049","position":"WR"},"redraftValue":111},{"player":{"name":"Dylan Sampson","sleeperId":"12469","position":"RB"},"redraftValue":99},{"player":{"name":"MarShawn Lloyd","sleeperId":"11581","position":"RB"},"redraftValue":94},{"player":{"name":"Jalen Nailor","sleeperId":"8180","position":"WR"},"redraftValue":96},{"player":{"name":"Daniel Jones","sleeperId":"5870","position":"QB"},"redraftValue":92},{"player":{"name":"Tank Dell","sleeperId":"9502","position":"WR"},"redraftValue":83},{"player":{"name":"Deebo Samuel","sleeperId":"5872","position":"WR"},"redraftValue":83},{"player":{"name":"Dalton Schultz","sleeperId":"5001","position":"TE"},"redraftValue":76},{"player":{"name":"Jerry Jeudy","sleeperId":"6783","position":"WR"},"redraftValue":65},{"player":{"name":"Braelon Allen","sleeperId":"11576","position":"RB"},"redraftValue":62},{"player":{"name":"Kimani Vidal","sleeperId":"11647","position":"RB"},"redraftValue":58},{"player":{"name":"David Njoku","sleeperId":"4033","position":"TE"},"redraftValue":59},{"player":{"name":"Ray Davis","sleeperId":"11575","position":"RB"},"redraftValue":51},{"player":{"name":"De'Zhaun Stribling","sleeperId":"13417","position":"WR"},"redraftValue":44},{"player":{"name":"Malik Willis","sleeperId":"8161","position":"QB"},"redraftValue":46},{"player":{"name":"Kaelon Black","sleeperId":"13414","position":"RB"},"redraftValue":37},{"player":{"name":"Kaytron Allen","sleeperId":"13405","position":"RB"},"redraftValue":37},{"player":{"name":"Emmett Johnson","sleeperId":"13337","position":"RB"},"redraftValue":35},{"player":{"name":"AJ Barner","sleeperId":"11603","position":"TE"},"redraftValue":36},{"player":{"name":"Chris Bell","sleeperId":"13311","position":"WR"},"redraftValue":34},{"player":{"name":"James Conner","sleeperId":"4137","position":"RB"},"redraftValue":33},{"player":{"name":"Trey Benson","sleeperId":"11589","position":"RB"},"redraftValue":29},{"player":{"name":"Bryce Young","sleeperId":"9228","position":"QB"},"redraftValue":27},{"player":{"name":"Kaleb Johnson","sleeperId":"12504","position":"RB"},"redraftValue":24},{"player":{"name":"Tre' Harris","sleeperId":"12509","position":"WR"},"redraftValue":25},{"player":{"name":"Antonio Williams","sleeperId":"13301","position":"WR"},"redraftValue":24},{"player":{"name":"Malik Washington","sleeperId":"11610","position":"WR"},"redraftValue":18},{"player":{"name":"Cade Otton","sleeperId":"8111","position":"TE"},"redraftValue":16},{"player":{"name":"Isaac TeSlaa","sleeperId":"12535","position":"WR"},"redraftValue":8},{"player":{"name":"Mike Washington","sleeperId":"13305","position":"RB"},"redraftValue":6},{"player":{"name":"Germie Bernard","sleeperId":"13274","position":"WR"},"redraftValue":7}];
const ALLTIME_DATA = null;
const TRANSACTIONS_DATA = [{"season":"2024","week":2,"managerA":"drjkay","managerB":"sanfbe","aReceives":[{"name":"Christian McCaffrey","pos":"RB"},{"name":"George Kittle","pos":"TE"}],"bReceives":[{"name":"DeVonta Smith","pos":"WR"},{"name":"Kyren Williams","pos":"RB"}]},{"season":"2024","week":3,"managerA":"joshjr11","managerB":"drjkay","aReceives":[{"name":"Deebo Samuel","pos":"WR"},{"name":"Dalton Kincaid","pos":"TE"}],"bReceives":[{"name":"Chris Godwin","pos":"WR"},{"name":"Zamir White","pos":"RB"}]},{"season":"2024","week":3,"managerA":"drjkay","managerB":"GSac","aReceives":[{"name":"Kenneth Walker","pos":"RB"},{"name":"Xavier Worthy","pos":"WR"}],"bReceives":[{"name":"Jordan Mason","pos":"RB"},{"name":"Rashid Shaheed","pos":"WR"}]},{"season":"2024","week":5,"managerA":"dpol","managerB":"GSac","aReceives":[{"name":"Davante Adams","pos":"WR"},{"name":"Jordan Mason","pos":"RB"}],"bReceives":[{"name":"Tony Pollard","pos":"RB"},{"name":"D'Andre Swift","pos":"RB"}]},{"season":"2024","week":9,"managerA":"dpol","managerB":"GSac","aReceives":[{"name":"Lamar Jackson","pos":"QB"},{"name":"Terry McLaurin","pos":"WR"},{"name":"Tony Pollard","pos":"RB"}],"bReceives":[{"name":"Alvin Kamara","pos":"RB"},{"name":"Sam Darnold","pos":"QB"},{"name":"Garrett Wilson","pos":"WR"}]},{"season":"2024","week":11,"managerA":"joshjr11","managerB":"drjkay","aReceives":[{"name":"Kenneth Walker","pos":"RB"},{"name":"Jayden Reed","pos":"WR"}],"bReceives":[{"name":"DK Metcalf","pos":"WR"},{"name":"James Cook","pos":"RB"}]},{"season":"2024","week":14,"managerA":"drjkay","managerB":"GSac","aReceives":[{"name":"Tee Higgins","pos":"WR"},{"name":"Jonathan Taylor","pos":"RB"}],"bReceives":[{"name":"Calvin Ridley","pos":"WR"},{"name":"De'Von Achane","pos":"RB"}]},{"season":"2024","week":14,"managerA":"benjlev","managerB":"dpol","aReceives":[{"name":"Tyreek Hill","pos":"WR"},{"name":"Tony Pollard","pos":"RB"}],"bReceives":[{"name":"George Pickens","pos":"WR"},{"name":"Brock Bowers","pos":"TE"}]},{"season":"2024","week":14,"managerA":"joshjr11","managerB":"sanfbe","aReceives":[{"name":"DeVonta Smith","pos":"WR"},{"name":"Rhamondre Stevenson","pos":"RB"}],"bReceives":[{"name":"Deebo Samuel","pos":"WR"},{"name":"Rico Dowdle","pos":"RB"}]},{"season":"2025","week":2,"managerA":"joshjr11","managerB":"sanfbe","aReceives":[],"bReceives":[{"name":"Cedric Tillman","pos":"WR"}]},{"season":"2025","week":4,"managerA":"joshjr11","managerB":"GSac","aReceives":[{"name":"Tony Pollard","pos":"RB"},{"name":"Brian Thomas","pos":"WR"}],"bReceives":[{"name":"DeVonta Smith","pos":"WR"},{"name":"Javonte Williams","pos":"RB"}]},{"season":"2025","week":6,"managerA":"drjkay","managerB":"sanfbe","aReceives":[{"name":"J.K. Dobbins","pos":"RB"}],"bReceives":[{"name":"DK Metcalf","pos":"WR"}]},{"season":"2025","week":6,"managerA":"dpol","managerB":"GSac","aReceives":[{"name":"Alvin Kamara","pos":"RB"},{"name":"CeeDee Lamb","pos":"WR"},{"name":"Tucker Kraft","pos":"TE"},{"name":"Quentin Johnston","pos":"WR"}],"bReceives":[{"name":"Davante Adams","pos":"WR"},{"name":"Jake Ferguson","pos":"TE"},{"name":"Ashton Jeanty","pos":"RB"}]},{"season":"2025","week":8,"managerA":"joshjr11","managerB":"drjkay","aReceives":[{"name":"Justin Jefferson","pos":"WR"}],"bReceives":[{"name":"Emeka Egbuka","pos":"WR"}]},{"season":"2025","week":8,"managerA":"joshjr11","managerB":"drjkay","aReceives":[{"name":"Michael Carter","pos":"RB"},{"name":"Chase Brown","pos":"RB"}],"bReceives":[{"name":"Tetairoa McMillan","pos":"WR"},{"name":"Jacory Croskey-Merritt","pos":"RB"}]},{"season":"2025","week":10,"managerA":"AlastairL","managerB":"GSac","aReceives":[{"name":"Khalil Shakir","pos":"WR"}],"bReceives":[{"name":"Cam Little","pos":"K"}]},{"season":"2025","week":10,"managerA":"dpol","managerB":"sanfbe","aReceives":[{"name":"Jaylen Warren","pos":"RB"}],"bReceives":[{"name":"Zay Flowers","pos":"WR"}]},{"season":"2025","week":11,"managerA":"dpol","managerB":"drjkay","aReceives":[{"name":"Omarion Hampton","pos":"RB"}],"bReceives":[{"name":"Jaylen Warren","pos":"RB"}]},{"season":"2025","week":12,"managerA":"dpol","managerB":"GSac","aReceives":[{"name":"Jonathan Taylor","pos":"RB"},{"name":"Juwan Johnson","pos":"TE"},{"name":"Javonte Williams","pos":"RB"}],"bReceives":[{"name":"Jauan Jennings","pos":"WR"},{"name":"James Cook","pos":"RB"},{"name":"Jahmyr Gibbs","pos":"RB"}]},{"season":"2025","week":13,"managerA":"dpol","managerB":"drjkay","aReceives":[{"name":"George Kittle","pos":"TE"},{"name":"Puka Nacua","pos":"WR"}],"bReceives":[{"name":"Jaxon Smith-Njigba","pos":"WR"},{"name":"Oronde Gadsden","pos":"TE"}]},{"season":"2025","week":13,"managerA":"dpol","managerB":"GSac","aReceives":[{"name":"Mike Evans","pos":"WR"},{"name":"Wan'Dale Robinson","pos":"WR"}],"bReceives":[{"name":"Christian Watson","pos":"WR"},{"name":"Tampa Bay Buccaneers","pos":"DEF"}]},{"season":"2025","week":13,"managerA":"dpol","managerB":"saulgoat","aReceives":[{"name":"Bucky Irving","pos":"RB"}],"bReceives":[{"name":"Javonte Williams","pos":"RB"}]}];
const PLAYER_SCORES = null;

const REPO = 'alastairlivingston-sudo/sleeper-league-app-3';
const PAGES_BASE = 'https://alastairlivingston-sudo.github.io/' + REPO.split('/')[1];

const DATA_SOURCES = [
  { history: 'https://cdn.jsdelivr.net/gh/' + REPO + '@main/docs/data/history.json', stats: 'https://cdn.jsdelivr.net/gh/' + REPO + '@main/docs/data/stats.json', rosters: 'https://cdn.jsdelivr.net/gh/' + REPO + '@main/docs/data/rosters.json', trades: 'https://cdn.jsdelivr.net/gh/' + REPO + '@main/docs/data/fc-values.json', alltime: 'https://cdn.jsdelivr.net/gh/' + REPO + '@main/docs/data/alltime.json', transactions: 'https://cdn.jsdelivr.net/gh/' + REPO + '@main/docs/data/transactions.json', players: 'https://cdn.jsdelivr.net/gh/' + REPO + '@main/docs/data/player-scores.json' },
  { history: PAGES_BASE + '/data/history.json', stats: PAGES_BASE + '/data/stats.json', rosters: PAGES_BASE + '/data/rosters.json', trades: PAGES_BASE + '/data/fc-values.json', alltime: PAGES_BASE + '/data/alltime.json', transactions: PAGES_BASE + '/data/transactions.json', players: PAGES_BASE + '/data/player-scores.json' },
];

const LORE_SOURCES = [
  { master: 'https://cdn.jsdelivr.net/gh/' + REPO + '@main/docs/lore/master.json', archive: 'https://cdn.jsdelivr.net/gh/' + REPO + '@main/docs/lore/archive-index.json', quotes: 'https://cdn.jsdelivr.net/gh/' + REPO + '@main/docs/lore/quotes-index.json' },
  { master: PAGES_BASE + '/lore/master.json', archive: PAGES_BASE + '/lore/archive-index.json', quotes: PAGES_BASE + '/lore/quotes-index.json' },
];
// jsDelivr sets a 7-day browser cache; append a daily buster so a returning
// user pulls fresh data each calendar day (the Action publishes once daily).
const BUST = '?v=' + new Date().toISOString().slice(0, 10);

const T = {
  bg: '#090d18', panel: '#0f1625', panel2: '#141e32', raised: '#1a2640',
  border: '#24344e', borderHi: '#304260', text: '#e8f1ff', dim: '#6e8db0',
  faint: '#3d5470', indigo: '#6366f1', indigoDk: '#4f46e5',
  green: '#10b981', amber: '#f59e0b', blue: '#60a5fa', red: '#f87171',
};
const FF = "-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,'Helvetica Neue',Arial,sans-serif";

// Calling the model from an artifact.
// The current Claude.ai artifact runtime is a sandboxed iframe (claudeusercontent.com)
// under a strict CSP: a raw fetch to api.anthropic.com is BLOCKED. The sanctioned
// bridge the host injects is window.claude.complete(promptString) -> string. It has
// no system/messages separation and no memory, so we flatten the whole exchange into
// one prompt string. (Older runtimes proxied the Messages API instead; that path is
// kept below as a fallback so the artifact works in both.)
const MODELS = ['claude-sonnet-5', 'claude-sonnet-4-5', 'claude-haiku-4-5'];
let modelIdx = 0;

function flattenPrompt(messages, systemPrompt) {
  const convo = (messages || [])
    .filter(function(m) { return m.role === 'user' || m.role === 'assistant'; })
    .map(function(m) { return (m.role === 'user' ? 'Human' : 'Assistant') + ': ' + m.content; })
    .join('\n\n');
  return systemPrompt + '\n\n' + convo + '\n\nAssistant:';
}

async function claudeCall(messages, systemPrompt) {
  // Primary: window.claude.complete (current artifact sandbox).
  if (typeof window !== 'undefined' && window.claude && typeof window.claude.complete === 'function') {
    const out = await window.claude.complete(flattenPrompt(messages, systemPrompt));
    if (!out) throw new Error('Empty response');
    return out;
  }
  // Fallback: older runtime that proxied the Messages API directly.
  return messagesApiCall(messages, systemPrompt);
}

async function messagesApiCall(messages, systemPrompt) {
  const body = (model) => JSON.stringify({
    model,
    max_tokens: 2000,
    system: systemPrompt,
    messages: messages
      .filter(function(m) { return m.role === 'user' || m.role === 'assistant'; })
      .map(function(m) { return { role: m.role, content: m.content }; }),
  });
  let lastErr = null;
  while (modelIdx < MODELS.length) {
    for (let attempt = 0; attempt < 3; attempt++) {
      const ctrl = new AbortController();
      const timer = setTimeout(function() { ctrl.abort(); }, 60000);
      try {
        const res = await fetch('https://api.anthropic.com/v1/messages', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: body(MODELS[modelIdx]),
          signal: ctrl.signal,
        });
        if (res.status === 404) { lastErr = new Error('MODEL_ERROR'); break; } // next model
        if (res.status === 429 || res.status >= 500) {
          lastErr = new Error('API ' + res.status);
          const ra = parseInt(res.headers.get('retry-after') || '0', 10);
          await new Promise(function(r) { setTimeout(r, ra > 0 ? ra * 1000 : (attempt + 1) * 2000); });
          continue; // retry same model
        }
        if (!res.ok) throw new Error('API ' + res.status);
        const data = await res.json();
        const text = (data.content || []).filter(function(b) { return b.type === 'text'; }).map(function(b) { return b.text; }).join('');
        if (!text) throw new Error('Empty response');
        return text;
      } catch (e) {
        if (e.message === 'MODEL_ERROR') break;
        lastErr = e;
        if (e.name === 'AbortError') continue; // timeout → retry
        throw e; // real network/4xx error — surface it
      } finally {
        clearTimeout(timer);
      }
    }
    if (lastErr && lastErr.message === 'MODEL_ERROR') { modelIdx++; lastErr = null; continue; }
    break; // retries exhausted on a live model
  }
  throw modelIdx >= MODELS.length ? new Error('MODEL_ERROR') : (lastErr || new Error('API failed'));
}

function r1(n) { return Math.round(n * 10) / 10; }
function r2(n) { return Math.round(n * 100) / 100; }
function stdev(arr) {
  if (arr.length < 2) return 0;
  const m = arr.reduce(function(a, b) { return a + b; }, 0) / arr.length;
  return Math.sqrt(arr.reduce(function(a, b) { return a + (b - m) * (b - m); }, 0) / arr.length);
}
function blankRow(h) { return { handle: h, w: 0, l: 0, t: 0, pf: 0, pa: 0, scores: [], apW: 0, apL: 0, apT: 0 }; }
function finalizeRow(m) {
  const games = m.w + m.l + m.t;
  const apG = m.apW + m.apL + m.apT;
  const apPct = apG ? m.apW / apG : 0;
  const expW = apPct * games;
  const avg = m.scores.length ? m.scores.reduce(function(a,b){return a+b;},0) / m.scores.length : 0;
  return {
    name: displayName(m.handle), alias: distinctName(m.handle), handle: m.handle,
    record: m.w + '-' + m.l + (m.t ? '-' + m.t : ''),
    wins: m.w, losses: m.l, pf: r2(m.pf), pa: r2(m.pa),
    avgScore: r1(avg), consistencySD: r1(stdev(m.scores)),
    high: m.scores.length ? r2(Math.max.apply(null, m.scores)) : 0,
    low: m.scores.length ? r2(Math.min.apply(null, m.scores)) : 0,
    allPlay: m.apW + '-' + m.apL + (m.apT ? '-' + m.apT : ''),
    allPlayWinPct: r1(apPct * 100), expectedWins: r1(expW), luck: r1(m.w - expW),
  };
}
function computeAnalytics(history) {
  const seasons = (history && history.seasons) ? history.seasons : [];
  const perSeason = [];
  const allAcc = {};
  for (let si = 0; si < seasons.length; si++) {
    const s = seasons[si];
    const acc = {};
    const weeks = {};
    const games = s.games || [];
    for (let gi = 0; gi < games.length; gi++) {
      const g = games[gi];
      if (g.playoff || !(g.pa > 0 || g.pb > 0)) continue;
      if (!weeks[g.week]) weeks[g.week] = [];
      weeks[g.week].push(g);
    }
    const weekKeys = Object.keys(weeks);
    for (let wi = 0; wi < weekKeys.length; wi++) {
      const wgames = weeks[weekKeys[wi]];
      const board = [];
      for (let gi = 0; gi < wgames.length; gi++) {
        const g = wgames[gi];
        const ha = canonical(g.a), hb = canonical(g.b);
        if (!acc[ha]) acc[ha] = blankRow(ha);
        if (!acc[hb]) acc[hb] = blankRow(hb);
        const A = acc[ha], B = acc[hb];
        A.pf += g.pa; A.pa += g.pb; A.scores.push(g.pa);
        B.pf += g.pb; B.pa += g.pa; B.scores.push(g.pb);
        if (g.pa > g.pb) { A.w++; B.l++; } else if (g.pb > g.pa) { B.w++; A.l++; } else { A.t++; B.t++; }
        board.push([ha, g.pa], [hb, g.pb]);
      }
      for (let ai = 0; ai < board.length; ai++) {
        const h = board[ai][0], sc = board[ai][1];
        const a = acc[h];
        for (let bi = 0; bi < board.length; bi++) {
          const h2 = board[bi][0], sc2 = board[bi][1];
          if (h2 === h) continue;
          if (sc > sc2) a.apW++; else if (sc < sc2) a.apL++; else a.apT++;
        }
      }
    }
    const rows = Object.values(acc);
    if (rows.length) {
      perSeason.push({
        season: s.season,
        champion: s.champion ? displayName(s.champion) : null,
        managers: rows.map(finalizeRow).sort(function(a,b){ return b.wins - a.wins || b.pf - a.pf; }),
      });
    }
    for (let ri = 0; ri < rows.length; ri++) {
      const m = rows[ri];
      if (!allAcc[m.handle]) allAcc[m.handle] = blankRow(m.handle);
      const t = allAcc[m.handle];
      t.w += m.w; t.l += m.l; t.t += m.t; t.pf += m.pf; t.pa += m.pa;
      t.apW += m.apW; t.apL += m.apL; t.apT += m.apT;
      for (let si2 = 0; si2 < m.scores.length; si2++) t.scores.push(m.scores[si2]);
    }
  }
  const allTime = Object.values(allAcc).map(finalizeRow).sort(function(a,b){ return b.wins - a.wins || b.pf - a.pf; });
  return { perSeason: perSeason, allTime: allTime };
}

// ── Deterministic stats query engine ─────────────────────────────────────────
// Flattens every game (canonicalised handles) into one list the query engine
// reads from. Never lets the model do arithmetic — JS computes, model narrates.
function flattenGames(history) {
  const seasons = (history && history.seasons) ? history.seasons : [];
  const rows = [];
  for (const s of seasons) {
    for (const g of (s.games || [])) {
      if (!(g.pa > 0 || g.pb > 0)) continue; // skip unplayed
      rows.push({ season: s.season, week: g.week, playoff: !!g.playoff, a: canonical(g.a), b: canonical(g.b), pa: g.pa, pb: g.pb });
    }
  }
  return rows;
}

// Executes a constrained query spec against flattened games. Pure + deterministic.
function runStatQuery(spec, games, players) {
  if (!spec || spec.type === 'none') return null;
  const regOnly = spec.regularSeasonOnly !== false; // default true
  const seasonSet = Array.isArray(spec.seasons) && spec.seasons.length ? new Set(spec.seasons.map(String)) : null;
  const mgrSet = Array.isArray(spec.managers) && spec.managers.length ? new Set(spec.managers.map(canonical)) : null;

  let pool = games.filter(function(g) {
    if (regOnly && g.playoff) return false;
    if (seasonSet && !seasonSet.has(String(g.season))) return false;
    return true;
  });

  if (spec.type === 'headToHead') {
    // Build record between every ordered pair, then filter to requested managers.
    const rec = {};
    function cell(x, y) { const k = x + '|' + y; if (!rec[k]) rec[k] = { manager: x, opponent: y, wins: 0, losses: 0, ties: 0, pf: 0, pa: 0, games: 0 }; return rec[k]; }
    for (const g of pool) {
      const A = cell(g.a, g.b), B = cell(g.b, g.a);
      A.pf += g.pa; A.pa += g.pb; A.games++;
      B.pf += g.pb; B.pa += g.pa; B.games++;
      if (g.pa > g.pb) { A.wins++; B.losses++; }
      else if (g.pb > g.pa) { B.wins++; A.losses++; }
      else { A.ties++; B.ties++; }
    }
    let rows = Object.values(rec);
    if (mgrSet) {
      if (mgrSet.size === 2) {
        const [m1, m2] = Array.from(mgrSet);
        rows = rows.filter(function(r) { return (r.manager === m1 && r.opponent === m2) || (r.manager === m2 && r.opponent === m1); });
      } else {
        rows = rows.filter(function(r) { return mgrSet.has(r.manager); });
      }
    }
    rows.forEach(function(r) { r.pf = Math.round(r.pf * 100) / 100; r.pa = Math.round(r.pa * 100) / 100; r.winPct = r.games ? Math.round(r.wins / r.games * 1000) / 1000 : 0; });
    rows.sort(function(x, y) { return x.manager.localeCompare(y.manager) || y.games - x.games; });
    return { type: 'headToHead', rows: rows.slice(0, 25) };
  }

  if (spec.type === 'totals') {
    const acc = {};
    function row(m) { if (!acc[m]) acc[m] = { manager: m, games: 0, wins: 0, losses: 0, ties: 0, pf: 0, pa: 0, high: 0, low: Infinity }; return acc[m]; }
    for (const g of pool) {
      if (mgrSet && !mgrSet.has(g.a) && !mgrSet.has(g.b)) continue;
      const A = row(g.a), B = row(g.b);
      A.games++; B.games++;
      A.pf += g.pa; A.pa += g.pb; B.pf += g.pb; B.pa += g.pa;
      A.high = Math.max(A.high, g.pa); A.low = Math.min(A.low, g.pa);
      B.high = Math.max(B.high, g.pb); B.low = Math.min(B.low, g.pb);
      if (g.pa > g.pb) { A.wins++; B.losses++; } else if (g.pb > g.pa) { B.wins++; A.losses++; } else { A.ties++; B.ties++; }
    }
    let rows = Object.values(acc);
    if (mgrSet) rows = rows.filter(function(r) { return mgrSet.has(r.manager); });
    rows.forEach(function(r) {
      r.pf = Math.round(r.pf * 100) / 100; r.pa = Math.round(r.pa * 100) / 100;
      r.avg = r.games ? Math.round(r.pf / r.games * 100) / 100 : 0;
      if (r.low === Infinity) r.low = 0;
      r.winPct = r.games ? Math.round(r.wins / r.games * 1000) / 1000 : 0;
    });
    rows.sort(function(x, y) { return y.wins - x.wins || y.pf - x.pf; });
    return { type: 'totals', rows: rows.slice(0, 25) };
  }

  if (spec.type === 'gameList') {
    let rows = pool.slice();
    if (mgrSet) rows = rows.filter(function(g) { return mgrSet.has(g.a) || mgrSet.has(g.b); });
    const by = spec.sortBy || 'combined';
    function metric(g) {
      if (by === 'margin') return Math.abs(g.pa - g.pb);
      if (by === 'high') return Math.max(g.pa, g.pb);
      if (by === 'low') return Math.min(g.pa, g.pb);
      return g.pa + g.pb; // combined
    }
    const order = spec.order === 'asc' ? 1 : -1;
    rows.sort(function(x, y) { return (metric(x) - metric(y)) * order; });
    const limit = Math.min(spec.limit || 10, 25);
    return { type: 'gameList', rows: rows.slice(0, limit) };
  }

  if (spec.type === 'player') {
    // Per-player, per-season starter scoring (name / pos / manager / season /
    // games / total pts / avg / single-game best). Queried in JS; only matching
    // rows are handed to the narrator.
    const nameNeedle = spec.player ? String(spec.player).toLowerCase() : null;
    const posSet = Array.isArray(spec.positions) && spec.positions.length ? new Set(spec.positions.map(function(p) { return String(p).toUpperCase(); })) : null;
    const by = spec.sortBy === 'avg' ? 'avg' : (spec.sortBy === 'best' ? 'best' : (spec.sortBy === 'games' ? 'games' : 'pts'));
    // avg is misleading on tiny samples, so require a floor unless the query set one.
    const minGames = spec.minGames != null ? spec.minGames : (by === 'avg' ? 4 : 1);
    let rows = (players || []).filter(function(r) {
      if (nameNeedle && String(r.player).toLowerCase().indexOf(nameNeedle) === -1) return false;
      if (posSet && !posSet.has(r.pos)) return false;
      if (mgrSet && !mgrSet.has(canonical(r.manager))) return false;
      if (seasonSet && !seasonSet.has(String(r.season))) return false;
      if (r.games < minGames) return false;
      return true;
    });
    const order = spec.order === 'asc' ? 1 : -1;
    rows = rows.slice().sort(function(x, y) { return (x[by] - y[by]) * order; });
    const limit = Math.min(spec.limit || 10, 25);
    return { type: 'player', rows: rows.slice(0, limit) };
  }

  return null;
}

// Session cache: identical questions cost the model nothing the second time.
const SPEC_CACHE = {};
function cacheKey(q) { return String(q || '').toLowerCase().replace(/\s+/g, ' ').trim(); }

// Which known managers does this query name? Matches distinct display names and
// first names (case-insensitive, word-boundary) → canonical handles, de-duped.
function matchManagers(query) {
  const q = ' ' + String(query || '').toLowerCase() + ' ';
  const hits = [];
  Object.keys(NAMES).forEach(function(h) {
    const names = [distinctName(h), displayName(h)];
    for (let i = 0; i < names.length; i++) {
      const full = String(names[i] || '').toLowerCase();
      if (!full) continue;
      const candidates = [full].concat(full.split(' ')); // full name + each token (first name)
      for (let j = 0; j < candidates.length; j++) {
        const w = candidates[j];
        if (w.length < 3) continue;
        if (new RegExp('(^|[^a-z])' + w.replace(/[.*+?^${}()|[\]\\]/g, '\\$&') + '([^a-z]|$)').test(q)) {
          if (hits.indexOf(h) === -1) hits.push(h);
          return; // this handle counted once
        }
      }
    }
  });
  return hits;
}

// Deterministic intent detection for the two common, unambiguous shapes, so the
// planner MODEL CALL is skipped on the majority path. Returns a spec or null
// (null → fall through to the model planner). Conservative by design: anything
// player/position-flavoured or ambiguous is left to the model.
const PLAYER_HINT = /\b(wr|rb|qb|te|def|kicker|\bk\b|player|players|scorer|scorers|scored|touchdown|position|positions)\b/i;
const H2H_HINT    = /\b(vs\.?|versus|against|head[\s-]?to[\s-]?head|h2h|beat|beaten|record against)\b/i;
const TOTALS_HINT = /\b(standings|all[\s-]?time (table|standings|record)|win totals?|most wins|best (record|team)|career (record|wins)|rankings?|league table)\b/i;
function fastPlan(query) {
  if (PLAYER_HINT.test(query)) return null;      // player scoring → model
  const mgrs = matchManagers(query);
  if (H2H_HINT.test(query) && mgrs.length >= 2) {
    return { type: 'headToHead', managers: mgrs };
  }
  if (TOTALS_HINT.test(query) && mgrs.length <= 1) {
    return { type: 'totals', managers: mgrs.length === 1 ? mgrs : undefined };
  }
  return null;
}

// Asks the model to translate a question into a query spec (JSON only).
// Fast-path (no model call) for common H2H/totals questions, then a session
// cache, then the model planner as the general fallback.
async function planStatQuery(query, messages) {
  const key = cacheKey(query);
  if (SPEC_CACHE[key]) return SPEC_CACHE[key];
  const fast = fastPlan(query);
  if (fast) { SPEC_CACHE[key] = fast; return fast; }

  const handles = Object.keys(NAMES);
  const nameLines = handles.map(function(h) { return h + '=' + distinctName(h); }).join(', ');
  const planner = [
    'You translate a fantasy-football question into a JSON query spec. Output ONLY valid JSON, nothing else.',
    'Manager handles (use these exact strings in "managers"): ' + nameLines + '. Note benjlev=Lev, sanfbe=Sanford, allyl900 maps to AlastairL.',
    'Schema:',
    '{ "type": "headToHead" | "totals" | "gameList" | "player" | "none",',
    '  "managers": [handle, ...]   // optional; for headToHead between two, list both',
    '  "seasons": ["2023", ...]    // optional; omit for all-time',
    '  "regularSeasonOnly": true,  // default true; set false to include playoffs',
    '  "sortBy": "margin"|"high"|"low"|"combined",  // gameList: margin/high/low/combined; player: "pts"|"avg"|"best"|"games"',
    '  "order": "desc"|"asc",      // gameList & player',
    '  "limit": 10,                // gameList & player',
    '  "player": "chase",          // player only: substring of the player name',
    '  "positions": ["WR"] }       // player only: filter by QB/RB/WR/TE/K/DEF',
    'headToHead with exactly two managers = the record between that pair. With three or more managers = each listed manager\'s record against ALL opponents. Omit "managers" for the whole league.',
    'Rules: head-to-head / nemesis / "who beats whom" -> headToHead. Manager records/standings/win totals/points -> totals. "biggest/closest/highest GAME" -> gameList. Individual NFL PLAYERS ("best WR", "top scorers", "most points by a QB", "how did Ja\'Marr Chase do", "who scored most for Lev") -> player (set "player" for a named player, "positions" for a position, "managers" for whose roster, "sortBy":"avg" for per-game rate vs "pts" for totals). If the question is not a numeric data lookup (opinion, definition, lore) -> {"type":"none"}.',
  ].join('\n');
  try {
    const raw = await claudeCall(messages.slice(-6), planner);
    const spec = parseSpec(raw);
    SPEC_CACHE[key] = spec;
    return spec;
  } catch (e) {
    return { type: 'none' };
  }
}

// Tolerant JSON extraction: whole reply → outermost braces → first {...} match.
function parseSpec(raw) {
  if (!raw) return { type: 'none' };
  try { return JSON.parse(raw.trim()); } catch (e) { /* not bare JSON */ }
  const first = raw.indexOf('{'), last = raw.lastIndexOf('}');
  if (first !== -1 && last > first) {
    try { return JSON.parse(raw.slice(first, last + 1)); } catch (e) { /* fall through */ }
  }
  const m = raw.match(/\{[\s\S]*\}/);
  if (m) { try { return JSON.parse(m[0]); } catch (e) { /* fall through */ } }
  return { type: 'none' };
}

// Renders a query result into a deterministic context block the narrator reads.
function formatQueryResult(spec, result) {
  if (!result || !result.rows || !result.rows.length) return '';
  function nm(h) { return distinctName(h); }
  const lines = [];
  if (result.type === 'headToHead') {
    lines.push('Head-to-head records (regular season unless stated):');
    result.rows.forEach(function(r) { lines.push('- ' + nm(r.manager) + ' vs ' + nm(r.opponent) + ': ' + r.wins + '-' + r.losses + (r.ties ? '-' + r.ties : '') + ', PF ' + r.pf + ' / PA ' + r.pa + ' (' + r.games + ' games)'); });
  } else if (result.type === 'totals') {
    lines.push('Aggregate records:');
    result.rows.forEach(function(r) { lines.push('- ' + nm(r.manager) + ': ' + r.wins + '-' + r.losses + (r.ties ? '-' + r.ties : '') + ', PF ' + r.pf + ', avg ' + r.avg + ', high ' + r.high + ', low ' + r.low + ' (' + r.games + ' games)'); });
  } else if (result.type === 'gameList') {
    lines.push('Matching games:');
    result.rows.forEach(function(g) { lines.push('- ' + g.season + ' wk' + g.week + (g.playoff ? ' (playoff)' : '') + ': ' + nm(g.a) + ' ' + g.pa + ' – ' + g.pb + ' ' + nm(g.b)); });
  } else if (result.type === 'player') {
    lines.push('Player scoring (starter points, per player · season · manager):');
    result.rows.forEach(function(p) { lines.push('- ' + p.player + ' (' + p.pos + ', ' + nm(p.manager) + ' ' + p.season + '): ' + p.pts + ' pts over ' + p.games + ' games, ' + p.avg + '/game, best ' + p.best); });
  }
  return '══ DETERMINISTIC QUERY RESULT (authoritative — narrate ONLY these numbers, do not recompute) ══\n' + lines.join('\n');
}

function useLeagueData() {
  const [data, setData] = useState({ history: HISTORY_DATA, stats: STATS_DATA, rosters: ROSTERS_DATA, trades: TRADE_VALUES, alltime: ALLTIME_DATA, transactions: TRANSACTIONS_DATA, players: PLAYER_SCORES, live: false });
  useEffect(function() {
    let cancelled = false;
    (async function() {
      for (let i = 0; i < DATA_SOURCES.length; i++) {
        try {
          const urls = DATA_SOURCES[i];
          const keys = Object.keys(urls);
          const fetched = await Promise.all(keys.map(function(k) {
            return fetch(urls[k] + BUST).then(function(r) { if (!r.ok) throw new Error(r.status); return r.json(); });
          }));
          const result = { live: true };
          keys.forEach(function(k, idx) { result[k] = fetched[idx]; });
          if (!cancelled) setData(result);
          return;
        } catch (e) { /* try next */ }
      }
    })();
    return function() { cancelled = true; };
  }, []);
  return data;
}

function useLore(active) {
  const [lore, setLore] = useState({ master: '', archive: [], quotes: [], ready: false });
  const fetchedRef = useRef(false);
  useEffect(function() {
    if (!active || fetchedRef.current) return;
    fetchedRef.current = true;
    let cancelled = false;
    (async function() {
      for (let i = 0; i < LORE_SOURCES.length; i++) {
        try {
          const urls = LORE_SOURCES[i];
          const [mData, aData, qData] = await Promise.all([
            fetch(urls.master + BUST).then(function(r) { if (!r.ok) throw new Error(r.status); return r.json(); }),
            fetch(urls.archive + BUST).then(function(r) { if (!r.ok) throw new Error(r.status); return r.json(); }),
            fetch(urls.quotes + BUST).then(function(r) { if (!r.ok) throw new Error(r.status); return r.json(); }),
          ]);
          if (!cancelled) setLore({ master: mData.text || '', archive: aData || [], quotes: qData || [], ready: true });
          return;
        } catch(e) { /* try next source */ }
      }
    })();
    return function() { cancelled = true; };
  }, [active]);
  return lore;
}

function retrieveLore(query, archive, quotes) {
  const MAX = 3500;
  if (!query || (!archive.length && !quotes.length)) return '';
  const STOP = new Set(['the','a','an','is','are','was','were','be','been','have','has','had','do','does','did','will','would','could','should','may','might','can','and','but','or','not','so','yet','no','only','than','too','very','just','now','for','with','about','from','into','before','after','out','over','under','then','once','in','on','at','by','of','to','up','down','off','me','my','we','our','you','your','he','his','she','her','it','its','they','them','their','what','which','who','this','that','these','those','some','any','all','when','where','how','why']);
  const tokens = query.toLowerCase().replace(/[^a-z0-9 ]/g, ' ').split(/\s+/).filter(function(t) { return t.length > 2 && !STOP.has(t); });
  if (!tokens.length) return '';

  const PMAP = {
    dan:'Daniel Polak', daniel:'Daniel Polak', polak:'Daniel Polak', commissioner:'Daniel Polak',
    saul:'Saul Freedman', freedman:'Saul Freedman', goat:'Saul Freedman',
    alastair:'Alastair Livingston', ally:'Alastair Livingston', livingston:'Alastair Livingston',
    gideon:'Gideon Sakofsky', sac:'Gideon Sakofsky', sakofsky:'Gideon Sakofsky',
    josh:'Josh Gaon', gaon:'Josh Gaon', trader:'Josh Gaon',
    jamie:'Jamie Kay', kay:'Jamie Kay', doctor:'Jamie Kay',
    lev:'Benjy Levey', levey:'Benjy Levey', autodraft:'Benjy Levey',
    sanford:'Benjy Sanford', snaff:'Benjy Sanford', darkwa:'Benjy Sanford',
  };
  const mentioned = new Set();
  tokens.forEach(function(t) { if (PMAP[t]) mentioned.add(PMAP[t]); });

  const parts = [];

  // Matching quote lines for mentioned people
  if (quotes.length && mentioned.size) {
    quotes.forEach(function(chunk) {
      if (mentioned.has(chunk.person)) {
        const sample = (chunk.lines || []).slice(0, 10).join('\n');
        if (sample) parts.push('QUOTES — ' + chunk.person + ':\n' + sample);
      }
    });
  }

  // Score archive chunks
  if (archive.length) {
    const yearMatch = query.match(/\b(201[7-9]|202[0-6])\b/);
    const scored = archive.map(function(chunk) {
      const haystack = (chunk.title + ' ' + chunk.text).toLowerCase();
      let score = tokens.reduce(function(s, t) { return s + (haystack.split(t).length - 1); }, 0);
      if (yearMatch && String(chunk.year) === yearMatch[1]) score += 8;
      mentioned.forEach(function(nm) {
        score += (haystack.split(nm.split(' ')[0].toLowerCase()).length - 1) * 2;
      });
      return { chunk: chunk, score: score };
    });
    scored.sort(function(a, b) { return b.score - a.score; });
    scored.slice(0, 2).forEach(function(x) {
      if (x.score > 1) parts.push('ARCHIVE — ' + x.chunk.title + ':\n' + x.chunk.text.slice(0, 1800));
    });
  }

  return parts.join('\n\n---\n\n').slice(0, MAX);
}

function useViewport() {
  const [keyboardOpen, setKb] = useState(false);
  useEffect(function() {
    const vv = window.visualViewport;
    function apply() {
      const h = vv ? vv.height : window.innerHeight;
      document.documentElement.style.setProperty('--app-h', h + 'px');
      setKb((window.innerHeight - h) > 120);
    }
    apply();
    if (vv) { vv.addEventListener('resize', apply); vv.addEventListener('scroll', apply); }
    window.addEventListener('resize', apply);
    window.addEventListener('orientationchange', apply);
    return function() {
      if (vv) { vv.removeEventListener('resize', apply); vv.removeEventListener('scroll', apply); }
      window.removeEventListener('resize', apply);
      window.removeEventListener('orientationchange', apply);
    };
  }, []);
  return { keyboardOpen: keyboardOpen };
}

function renderInline(str, kb) {
  return str.split(/(\*\*[^*]+\*\*)/g).map(function(p, i) {
    if (/^\*\*[^*]+\*\*$/.test(p)) return React.createElement('strong', { key: kb + '-' + i }, p.slice(2, -2));
    return React.createElement('span', { key: kb + '-' + i }, p);
  });
}
function splitRow(line) {
  return line.replace(/^\s*\|/, '').replace(/\|\s*$/, '').split('|').map(function(c) { return c.trim(); });
}
function MarkdownMessage(props) {
  const lines = String(props.text || '').split('\n');
  const out = [];
  let i = 0, key = 0;
  function isRow(l) { return /^\s*\|.*\|\s*$/.test(l); }
  function isSep(l) { return /^\s*\|[\s:|-]+\|\s*$/.test(l); }
  while (i < lines.length) {
    const line = lines[i];
    if (isRow(line) && i + 1 < lines.length && isSep(lines[i + 1])) {
      const header = splitRow(line);
      i += 2;
      const rows = [];
      while (i < lines.length && isRow(lines[i]) && !isSep(lines[i])) { rows.push(splitRow(lines[i])); i++; }
      const tbl = React.createElement('div', { key: key++, style: { overflowX: 'auto', margin: '8px 0' } },
        React.createElement('table', { className: 'md-table' },
          React.createElement('thead', null, React.createElement('tr', null, header.map(function(h, j) { return React.createElement('th', { key: j }, renderInline(h, 'h' + j)); }))),
          React.createElement('tbody', null, rows.map(function(r, ri) { return React.createElement('tr', { key: ri }, r.map(function(c, ci) { return React.createElement('td', { key: ci }, renderInline(c, ri + '-' + ci)); })); }))
        )
      );
      out.push(tbl);
      continue;
    }
    if (line.trim() === '') { i++; continue; }
    const para = [];
    while (i < lines.length && lines[i].trim() !== '' && !(isRow(lines[i]) && i + 1 < lines.length && isSep(lines[i + 1]))) { para.push(lines[i]); i++; }
    const children = [];
    for (let li = 0; li < para.length; li++) {
      const inlined = renderInline(para[li], key + '-' + li);
      inlined.forEach(function(el) { children.push(el); });
      if (li < para.length - 1) children.push(React.createElement('br', { key: 'br-' + li }));
    }
    out.push(React.createElement('p', { key: key++, style: { margin: '0 0 7px', lineHeight: 1.55 } }, children));
  }
  return React.createElement(React.Fragment, null, out);
}

function ChatTab(props) {
  const { systemPrompt, chips, placeholder, errorMsg, intro, buildContext } = props;
  const [messages, setMessages] = useState([]);
  const [input, setInput] = useState('');
  const [loading, setLoading] = useState(false);
  const bottomRef = useRef(null);
  const taRef = useRef(null);

  useEffect(function() { if (bottomRef.current) bottomRef.current.scrollIntoView({ behavior: 'smooth' }); }, [messages, loading]);

  function autoResize() {
    const ta = taRef.current;
    if (!ta) return;
    ta.style.height = 'auto';
    ta.style.height = Math.min(ta.scrollHeight, 120) + 'px';
  }

  async function send(text) {
    const t = (text || input).trim();
    if (!t || loading) return;
    const next = messages.concat([{ role: 'user', content: t }]);
    setMessages(next); setInput(''); setLoading(true);
    if (taRef.current) taRef.current.style.height = 'auto';
    try {
      const extra = buildContext ? await buildContext(t, next) : '';
      const fullPrompt = extra ? systemPrompt + '\n\n' + extra : systemPrompt;
      const raw = await claudeCall(next.filter(function(m) { return m.role === 'user' || m.role === 'assistant'; }), fullPrompt);
      const reply = raw.replace(/\[(FACT|MYTH|REAL|EVENT)\]/g, '').replace(/  +/g, ' ').trim();
      setMessages(function(p) { return p.concat([{ role: 'assistant', content: reply }]); });
    } catch (e) {
      const txt = (e && e.message === 'MODEL_ERROR') ? 'Model hiccup — try again.' : errorMsg;
      setMessages(function(p) { return p.concat([{ role: 'error', content: txt }]); });
    } finally { setLoading(false); }
  }

  const bubUser = { alignSelf: 'flex-end', background: 'linear-gradient(135deg,#6366f1,#4f46e5)', color: '#fff', borderRadius: '18px 18px 4px 18px', padding: '11px 15px', maxWidth: '82%', fontSize: 15, lineHeight: 1.5, whiteSpace: 'pre-wrap', boxShadow: '0 3px 14px rgba(99,102,241,0.3)' };
  const bubAsst = { alignSelf: 'flex-start', background: '#141e32', color: '#e8f1ff', border: '1px solid #24344e', borderLeft: '3px solid #6366f1', borderRadius: '4px 18px 18px 18px', padding: '12px 15px', maxWidth: '88%', fontSize: 15, lineHeight: 1.6 };
  const bubErr  = { alignSelf: 'flex-start', background: '#141e32', color: '#f59e0b', border: '1px solid #24344e', borderLeft: '3px solid #f59e0b', borderRadius: '4px 18px 18px 18px', padding: '12px 15px', maxWidth: '88%', fontSize: 15, whiteSpace: 'pre-wrap' };

  return (
    <div style={{ display: 'flex', flexDirection: 'column', height: '100%', background: T.bg }}>
      <div style={{ display: 'flex', gap: 8, padding: '10px 14px', borderBottom: '1px solid ' + T.border, overflowX: 'auto', flexShrink: 0, background: T.panel }}>
        {chips.map(function(c, i) {
          return <button key={i} className="chip" onClick={function() { send(c); }} disabled={loading}>{c}</button>;
        })}
      </div>
      <div style={{ flexGrow: 1, overflowY: 'auto', padding: 16, display: 'flex', flexDirection: 'column', gap: 12 }}>
        {messages.length === 0 && !loading && (
          <div style={{ margin: 'auto', textAlign: 'center', color: T.faint, fontSize: 14, maxWidth: 300, lineHeight: 1.7, padding: '20px 8px' }}>{intro}</div>
        )}
        {messages.map(function(m, i) {
          if (m.role === 'assistant') return <div key={i} style={bubAsst}><MarkdownMessage text={m.content} /></div>;
          if (m.role === 'error') return <div key={i} style={bubErr}>{m.content}</div>;
          return <div key={i} style={bubUser}>{m.content}</div>;
        })}
        {loading && (
          <div style={bubAsst}>
            <span className="ld" /><span className="ld" /><span className="ld" />
          </div>
        )}
        <div ref={bottomRef} />
      </div>
      <div style={{ display: 'flex', gap: 8, padding: '10px 12px', borderTop: '1px solid ' + T.border, background: T.panel, flexShrink: 0 }}>
        <textarea ref={taRef} rows={1} value={input}
          onChange={function(e) { setInput(e.target.value); }}
          onInput={autoResize}
          onKeyDown={function(e) { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); send(); } }}
          placeholder={placeholder}
          style={{ flex: 1, background: T.raised, color: T.text, border: '1px solid ' + T.borderHi, borderRadius: 11, padding: '12px 13px', fontSize: 16, resize: 'none', outline: 'none', lineHeight: 1.4 }}
        />
        <button onClick={function() { send(); }} disabled={loading || !input.trim()} className="send-btn" style={{ opacity: (loading || !input.trim()) ? 0.4 : 1 }}>SEND</button>
      </div>
    </div>
  );
}

function slimAlltime(d) {
  if (!d) return d;
  // weeklyScores (raw per-game scores) and h2hBySeason (covered by deterministic query layer)
  // are excluded to keep the system prompt within the artifact runtime's prompt budget.
  // records games have player-name arrays stripped — scores and teams are sufficient for narration.
  function stripGame(g) {
    if (!g || typeof g !== 'object') return g;
    const { as, bs, ab, bb, ...rest } = g;
    return rest;
  }
  const records = {};
  for (const [k, v] of Object.entries(d.records || {})) {
    records[k] = Array.isArray(v) ? v.map(stripGame) : stripGame(v);
  }
  const { weeklyScores, h2hBySeason, records: _r, ...rest } = d;
  return { ...rest, records };
}

// ── Compact prompt encoders (token-lean: no repeated JSON keys) ───────────────
// Pipe-delimited tables beat JSON for tabular data — one header line, then rows,
// so a field name is paid for once instead of once per manager. The big matrices
// (allTimeH2H, weeklyScores, per-season boards) are dropped from the prompt
// entirely — the deterministic query engine answers those exactly on demand.
function tsv(cols, rows) {
  return [cols.join('|')].concat((rows || []).map(function(r) {
    return cols.map(function(c) { const v = r[c]; return v == null ? '' : v; }).join('|');
  })).join('\n');
}
// Career table from computeAnalytics().allTime — carries record, pf/pa, avg, the
// consistency SD, all-time high/low, all-play and the luck (actual−expected wins).
function careerTable(allTime) {
  return tsv(['name', 'rec', 'pf', 'pa', 'avg', 'SD', 'hi', 'lo', 'allPlay', 'apW%', 'xW', 'luck'],
    (allTime || []).map(function(m) {
      return { name: m.alias || m.name, rec: m.record, pf: m.pf, pa: m.pa, avg: m.avgScore,
        SD: m.consistencySD, hi: m.high, lo: m.low, allPlay: m.allPlay, 'apW%': m.allPlayWinPct,
        xW: m.expectedWins, luck: m.luck };
    }));
}
// Everything from alltime.json that the career table does NOT already cover,
// encoded compactly: playoff records, nemesis/bunny, all-time record games,
// per-season finish trend, and positional strengths.
function alltimeSummary(d) {
  if (!d) return '';
  const out = [];
  const g = function(x) { return x ? (distinctName(x.a) + ' ' + x.pa + '-' + x.pb + ' ' + distinctName(x.b) + ' [' + x.season + ' wk' + x.week + ']') : ''; };

  const pr = d.playoffRecords || {};
  const prRows = Object.keys(pr).map(function(h) { const r = pr[h]; return { name: distinctName(h), rec: r.wins + '-' + r.losses, apps: r.appearances, pf: r.pf, pa: r.pa }; });
  if (prRows.length) out.push('PLAYOFFS (W-L, apps=appearances):\n' + tsv(['name', 'rec', 'apps', 'pf', 'pa'], prRows));

  if (d.nemesis) out.push('NEMESIS (worst matchup, min 3 mtgs): ' + Object.keys(d.nemesis).map(function(h) { const n = d.nemesis[h]; return distinctName(h) + '→' + distinctName(n.opponent) + '(' + n.wins + '-' + n.losses + ')'; }).join('; '));
  if (d.bunny)   out.push('BUNNY (best matchup, min 3 mtgs): '   + Object.keys(d.bunny).map(function(h)   { const b = d.bunny[h];   return distinctName(h) + '→' + distinctName(b.opponent) + '(' + b.wins + '-' + b.losses + ')'; }).join('; '));

  const rec = d.records || {};
  const rl = [];
  if (rec.highestScore)  rl.push('highest game: ' + g(rec.highestScore));
  if (rec.lowestScore)   rl.push('lowest game: ' + g(rec.lowestScore));
  if (rec.biggestWin)    rl.push('biggest blowout: ' + g(rec.biggestWin));
  if (rec.closestGame)   rl.push('closest game: ' + g(rec.closestGame));
  if (rec.highestSeason) rl.push('highest season PF: ' + distinctName(rec.highestSeason.manager) + ' ' + rec.highestSeason.pf + ' (' + rec.highestSeason.season + ')');
  if (rec.lowestSeason)  rl.push('lowest season PF: ' + distinctName(rec.lowestSeason.manager) + ' ' + rec.lowestSeason.pf + ' (' + rec.lowestSeason.season + ')');
  if (rec.longestWinStreak)  rl.push('longest W streak: ' + distinctName(rec.longestWinStreak.manager) + ' ' + rec.longestWinStreak.streak);
  if (rec.longestLossStreak) rl.push('longest L streak: ' + distinctName(rec.longestLossStreak.manager) + ' ' + rec.longestLossStreak.streak);
  if (rl.length) out.push('RECORDS:\n' + rl.join('\n'));

  // Per-season finish trend: "name: 2023 r3 108ppg, 2024 r1 121ppg, ..."
  if (d.seasonalTrends) {
    const tl = Object.keys(d.seasonalTrends).map(function(h) {
      const arr = d.seasonalTrends[h] || [];
      return distinctName(h) + ': ' + arr.map(function(t) { return t.season + ' r' + t.rank + ' ' + t.ppg + 'ppg'; }).join(', ');
    });
    if (tl.length) out.push('SEASON TREND (r=finish rank, ppg=points/game):\n' + tl.join('\n'));
  }

  // Positional strengths: latest season only, avg pts/game by position.
  if (d.positionalStrengths) {
    const rows = [];
    Object.keys(d.positionalStrengths).forEach(function(h) {
      const seasonsObj = d.positionalStrengths[h] || {};
      const yrs = Object.keys(seasonsObj).sort();
      const last = yrs[yrs.length - 1];
      if (!last) return;
      const p = seasonsObj[last];
      rows.push({ name: distinctName(h), yr: last, QB: p.QB, RB: p.RB, WR: p.WR, TE: p.TE, K: p.K, DEF: p.DEF });
    });
    if (rows.length) out.push('POSITIONAL avg pts/game (latest season):\n' + tsv(['name', 'yr', 'QB', 'RB', 'WR', 'TE', 'K', 'DEF'], rows));
  }
  return out.join('\n\n');
}
// One line per completed trade — compact vs JSON's repeated keys.
function tradesLines(txns) {
  const list = Array.isArray(txns) ? txns : ((txns && txns.items) || []);
  if (!list.length) return '(none on record)';
  return list.map(function(t) {
    const a = (t.aReceives || []).join(', '), b = (t.bReceives || []).join(', ');
    return t.season + ' wk' + t.week + ': ' + distinctName(t.managerA) + ' gets [' + a + '] ↔ ' + distinctName(t.managerB) + ' gets [' + b + ']';
  }).join('\n');
}
// Current-season standings + extremes, compact.
function currentSeasonBlock(statsData) {
  if (!statsData) return '(no current-season data)';
  const st = (statsData.standings || []).map(function(r, i) {
    return { rk: i + 1, name: distinctName(r.manager), rec: (r.wins || 0) + '-' + (r.losses || 0), pf: r.pf, pa: r.pa };
  });
  let s = 'season ' + statsData.season + '\n' + tsv(['rk', 'name', 'rec', 'pf', 'pa'], st);
  if (statsData.extremes) s += '\nextremes: ' + JSON.stringify(statsData.extremes);
  return s;
}

// Per-season champion + condensed standings (no games) — for the banter prompt.
function slimHistory(d) {
  const seasons = (d && d.seasons) ? d.seasons : [];
  return seasons.map(function(s) {
    return {
      season: s.season,
      champion: s.champion ? distinctName(s.champion) : null,
      standings: (s.standings || []).map(function(r) { return { manager: distinctName(r.manager), wins: r.wins, losses: r.losses, pf: r.pf }; }),
    };
  });
}

function StatsTab(props) {
  const { historyData, statsData, alltimeData, transactionsData, playersData, loreMaster } = props;
  const analytics = useMemo(function() { return computeAnalytics(historyData); }, [historyData]);
  const games = useMemo(function() { return flattenGames(historyData); }, [historyData]);
  const loreCtx = loreMaster
    ? '\n\nLEAGUE CONTEXT (names, champions, glossary — for resolving names only, NOT for inventing stats):\n' + loreMaster.slice(0, 2500)
    : '';
  // Narration prompt: deterministic pre-computed tables ONLY — no raw game dump,
  // so the model cannot do its own (error-prone) win/loss arithmetic. Anything
  // not in the tables arrives via the DETERMINISTIC QUERY RESULT block (buildContext).
  const sp = 'You are the statistician for the Borehamwood Plancy League. Answer ONLY from the data below — NEVER compute or invent numbers yourself.\n\n'
    + 'CRITICAL: All win/loss/points figures must be read verbatim from the tables below or the DETERMINISTIC QUERY RESULT block. Do NOT add up games or derive records yourself — if a number is not given to you, say you do not have it rather than estimate.\n\n'
    + 'NAMES: always use the real NAME shown in the tables, never a Sleeper handle. Two Benjys: Lev and Sanford.\n\n'
    + 'ANSWER FORMAT: 1) ONE short sentence on method. 2) GitHub Markdown table for any ranking. 3) At most ONE closing sentence. No preamble.\n\n'
    + 'TABLES BELOW are pipe-delimited (header row, then one row per manager). Column key — '
    + 'rec=record W-L; pf/pa=points for/against; avg=avg score; SD=consistency (lower=steadier); hi/lo=best/worst single game; '
    + 'allPlay=record vs the whole league each week; apW%=all-play win%; xW=expected wins; luck=actual−expected wins (+lucky, −unlucky).\n\n'
    + 'ANYTHING NOT IN THESE TABLES — a specific head-to-head, a per-season H2H, a game list, individual NFL player scoring — arrives via the DETERMINISTIC QUERY RESULT block appended below when relevant. Do not guess it from the tables.'
    + loreCtx
    + '\n\nCAREER (all-time, regular season):\n' + careerTable(analytics.allTime)
    + '\n\n' + alltimeSummary(slimAlltime(alltimeData))
    + '\n\nTRADES:\n' + tradesLines(transactionsData)
    + '\n\nCURRENT SEASON:\n' + currentSeasonBlock(statsData);

  // buildContext: deterministic query layer. Plan → execute in JS → inject result.
  // Plans from the running conversation (not just the latest line) so follow-ups
  // like "and in 2024?" resolve their referents.
  async function buildContext(query, conversation) {
    try {
      const convo = (conversation && conversation.length) ? conversation : [{ role: 'user', content: query }];
      const spec = await planStatQuery(query, convo);
      const result = runStatQuery(spec, games, playersData);
      return formatQueryResult(spec, result);
    } catch (e) { return ''; }
  }

  return <ChatTab systemPrompt={sp} buildContext={buildContext} chips={['All-time standings', 'Unluckiest manager?', 'Most consistent scorer?', 'Biggest bench disasters?']} placeholder="Message…" errorMsg="Something went wrong — try again." intro="The record book — career records, luck ratings, playoff history, consistency. Ask about personal bests, positional strengths, or any head-to-head." />;
}

function BanterTab(props) {
  const { historyData, statsData, alltimeData, lore } = props;

  const banterPrompt = useMemo(function() {
    const rule = [
      '══ PRIME DIRECTIVE ══',
      'You are the resident wind-up merchant of the Borehamwood Plancy League.',
      'STYLE: ESPN mock-grandeur, bone-dry British wit, mock gravity. Punchy 3-5 sentences. Lean on Jewish/Borehamwood texture. Sign off big beats with "a hearty hearty mazel tov."',
      'Never be cruel — mockery is the love language. Never invent statistics.',
      '',
      'CRITICAL OUTPUT RULE: NEVER print tag labels like [FACT], [MYTH], [REAL], or [EVENT] in your response. These are internal reasoning cues only — your output must read as natural chat, no square-bracket annotations of any kind.',
      '',
      'VERIFICATION RULE: Before stating any league result, score, record, or season outcome, verify it against RAW HISTORY and CURRENT SEASON below. If the event predates our data (pre-2023), you may reference it but must treat it as unverified lore rather than confirmed fact. Never contradict the data.',
      '',
      '══ LORE MASTER ══',
    ].join('\n');

    // Slim summaries only — raw JSON dumps blew the prompt budget (the repo's
    // own <20 KB rule). Champions + standings, all-time records, current-season
    // standings/extremes are enough to verify any claim the bot makes.
    const statsSummary = statsData ? { league: statsData.league, season: statsData.season, standings: statsData.standings, extremes: statsData.extremes } : null;
    const dataSection = '\n\n══ VERIFIED DATA (check all facts here first) ══'
      + '\n\nSEASON HISTORY (champions + standings):\n' + JSON.stringify(slimHistory(historyData))
      + '\n\nCURRENT SEASON:\n' + JSON.stringify(statsSummary)
      + '\n\nALL-TIME RECORDS:\n' + JSON.stringify(slimAlltime(alltimeData));
    if (DEBUG) console.log('banter prompt bytes:', (rule + (lore.master || '') + dataSection).length);

    if (lore.master) return rule + '\n' + lore.master + dataSection;

    // Fallback when lore files haven't loaded yet
    return rule + '\nChampions (most recent first): Alastair Livingston 2025 (first legit Plancey — beat Polak in final), Saul Freedman 2024+2023+2021+2017 (4 belts, the GOAT), Benjy Levey 2022, Jamie Kay 2020, Josh Gaon 2019, Gideon Sakofsky 2018, Alastair Livingston inaugural (asterisked).'
      + '\nThe Miriam = wooden spoon. Sanford = king of the Miriam. Lev = autodraft legend ("Does it auto-draft automatically?"). Dan = Commissioner/narrator. Gideon lives in Israel (4am watches). The Plancey = championship belt named after Rabbi Alan Plancey.'
      + '\nTWO BENJYS: benjlev=Lev, sanfbe=Sanford. allyl900=Alastair.'
      + '\nNAME MAP: ' + JSON.stringify(NAMES)
      + dataSection;
  }, [lore.master, historyData, statsData, alltimeData]);

  function buildContext(query) {
    if (!lore.ready) return '';
    const lore_ctx = retrieveLore(query, lore.archive, lore.quotes);
    return lore_ctx ? '--- RETRIEVED LORE ---\n' + lore_ctx : '';
  }

  return <ChatTab
    systemPrompt={banterPrompt}
    buildContext={buildContext}
    chips={['Roast the 2025 champion', 'Most cursed manager?', "This week's smack bulletin", 'Roast Lev']}
    placeholder="Message…"
    errorMsg="Blimey — give it another go."
    intro="Pull up a chair. Ask for a roast, a smack-talk bulletin, or just stir the pot."
  />;
}

function normalizeName(str) { return str.toLowerCase().replace(/[^a-z0-9 ]/g, '').replace(/\s+/g, ' ').trim(); }

function TradeGrader(props) {
  const { rostersData, tradeValues } = props;
  const teamKeys = useMemo(function() { return Object.keys(rostersData || {}); }, [rostersData]);
  const [teamA, setTeamA] = useState(teamKeys[0] || '');
  const [teamB, setTeamB] = useState(teamKeys[1] || '');
  const [sideAText, setSideAText] = useState('');
  const [sideBText, setSideBText] = useState('');
  const [result, setResult] = useState(null);
  const [verdict, setVerdict] = useState(null);
  const [vLoading, setVL] = useState(false);
  const [vError, setVE] = useState(false);
  const [showWaiver, setShowWaiver] = useState(true);

  useEffect(function() {
    if (document.getElementById('tg-spin')) return;
    const s = document.createElement('style');
    s.id = 'tg-spin';
    s.textContent = '@keyframes tg-spin{to{transform:rotate(360deg)}}';
    document.head.appendChild(s);
  }, []);

  const { idMap, nameMap } = useMemo(function() {
    const id = new Map(), nm = new Map();
    (tradeValues || []).forEach(function(item) {
      const p = item.player, val = item.redraftValue || 0;
      if (p.sleeperId) id.set(p.sleeperId, { value: val, officialName: p.name, position: p.position });
      nm.set(normalizeName(p.name), { value: val, sleeperId: p.sleeperId, officialName: p.name, position: p.position });
    });
    return { idMap: id, nameMap: nm };
  }, [tradeValues]);

  const waiverByPos = useMemo(function() {
    const rostered = new Set(Object.values(rostersData || {}).reduce(function(acc, team) { return acc.concat(team.map(function(p) { return p.id; })); }, []));
    const avail = (tradeValues || []).filter(function(x) { return x.player.sleeperId && !rostered.has(x.player.sleeperId); });
    const byPos = {};
    avail.forEach(function(p) {
      const pos = p.player.position;
      if (!byPos[pos]) byPos[pos] = [];
      if (byPos[pos].length < 5) byPos[pos].push(p);
    });
    return byPos;
  }, [rostersData, tradeValues]);

  function lookupPlayer(txt) {
    const n = normalizeName(txt);
    const hit = function(e) { return { found: true, value: e.value, officialName: e.officialName, position: e.position }; };
    // 1) exact
    if (nameMap.has(n)) return hit(nameMap.get(n));
    if (n.length < 2) return { found: false, value: 0, officialName: txt, position: null };
    // 2) prefix match (only for reasonably specific input) — "josh alle" -> "josh allen"
    if (n.length >= 4) { for (const [k, e] of nameMap) { if (k.startsWith(n)) return hit(e); } }
    // 3) all input tokens present in the name — "allen josh" -> "josh allen"
    const toks = n.split(' ').filter(Boolean);
    for (const [k, e] of nameMap) {
      if (toks.every(function(t) { return k.indexOf(t) !== -1; })) return hit(e);
    }
    return { found: false, value: 0, officialName: txt, position: null };
  }

  function gradeTrade() {
    function parse(txt) { return txt.split('\n').map(function(l) { return l.trim(); }).filter(Boolean).map(function(line) { return Object.assign({ input: line }, lookupPlayer(line)); }); }
    const sA = parse(sideAText), sB = parse(sideBText);
    const tA = sA.reduce(function(s, p) { return s + p.value; }, 0);
    const tB = sB.reduce(function(s, p) { return s + p.value; }, 0);
    const gap = Math.abs(tA - tB), gapPct = (gap / Math.max(tA, tB, 1)) * 100;
    const winner = gapPct < 5 ? 'even' : tA > tB ? 'A' : 'B';
    const tier = gapPct < 5 ? 'DEAD EVEN' : gapPct < 12 ? 'SLIGHT EDGE' : gapPct < 25 ? 'CLEAR WINNER' : 'LOPSIDED';
    const icon = gapPct < 5 ? '⚖️' : gapPct < 12 ? '📊' : gapPct < 25 ? '🏆' : '🚨';
    let addOns = [];
    if (winner !== 'even') {
      const wTeam = winner === 'A' ? teamA : teamB;
      const wSide = winner === 'A' ? sA : sB;
      const exclude = new Set(wSide.map(function(p) { return p.officialName; }));
      addOns = ((rostersData || {})[wTeam] || [])
        .map(function(rp) { return { name: idMap.has(rp.id) ? idMap.get(rp.id).officialName : rp.name, value: idMap.has(rp.id) ? idMap.get(rp.id).value : 0 }; })
        .filter(function(rp) { return !exclude.has(rp.name) && rp.value > 0; })
        .sort(function(a, b) { return Math.abs(a.value - gap) - Math.abs(b.value - gap); })
        .slice(0, 3);
    }
    const positions = new Set(sA.concat(sB).map(function(p) { return p.position; }).filter(Boolean));
    const waiverContext = {};
    positions.forEach(function(pos) { waiverContext[pos] = (waiverByPos[pos] || []).slice(0, 3); });
    setResult({ sA: sA, sB: sB, tA: tA, tB: tB, winner: winner, tier: tier, icon: icon, gap: gap, gapPct: gapPct, addOns: addOns, waiverContext: waiverContext });
    setVerdict(null); setVE(false);
  }

  async function getVerdict() {
    if (!result) return;
    setVL(true); setVE(false); setVerdict(null);
    const sys = "You are the wind-up merchant of the Borehamwood Plancy League. Quick funny verdict in 2-4 sentences. Bone-dry British banter. Use real names not team names. Numbers are computed — do NOT recalculate.";
    const margin = result.winner === 'even' ? 'Even.' : ('Side ' + result.winner + ' wins by ' + result.gap.toFixed(0) + ' (' + result.gapPct.toFixed(0) + '%)');
    const addOnStr = result.addOns.length ? ' Suggested add-on: ' + result.addOns.map(function(a) { return a.name; }).join(' or ') + '.' : '';
    const sAStr = result.sA.map(function(p) { return p.officialName + '(' + p.value + ')'; }).join(', ');
    const sBStr = result.sB.map(function(p) { return p.officialName + '(' + p.value + ')'; }).join(', ');
    const msg = 'Trade:\nSide A (' + teamLabel(teamA) + ') gives: ' + sAStr + ' — Total ' + result.tA + '\nSide B (' + teamLabel(teamB) + ') gives: ' + sBStr + ' — Total ' + result.tB + '\nVerdict: ' + result.tier + '. ' + margin + addOnStr;
    try {
      const r = await claudeCall([{ role: 'user', content: msg }], sys);
      setVerdict(r);
    } catch (e) { setVE(true); }
    finally { setVL(false); }
  }

  const teamOpts = teamKeys.map(function(k) { return <option key={k} value={k}>{teamLabel(k)}</option>; });
  const winA = result && result.winner === 'A';
  const winB = result && result.winner === 'B';
  const spin = { width: 14, height: 14, border: '2px solid ' + T.amber, borderTopColor: 'transparent', borderRadius: '50%', display: 'inline-block', animation: 'tg-spin 0.8s linear infinite' };

  return (
    <div style={{ background: T.bg, color: T.text, overflowY: 'auto', height: '100%', boxSizing: 'border-box' }}>
      <div style={{ borderBottom: '1px solid ' + T.border, background: T.panel }}>
        <button onClick={function() { setShowWaiver(function(v) { return !v; }); }} style={{ width: '100%', display: 'flex', alignItems: 'center', justifyContent: 'space-between', padding: '11px 15px', background: 'none', border: 'none', color: T.dim, cursor: 'pointer', fontSize: 12.5, fontWeight: 700, letterSpacing: '0.07em', textTransform: 'uppercase' }}>
          <span>Waiver Wire <span style={{ color: T.faint, fontSize: 11, fontWeight: 400 }}>(top available by position)</span></span>
          <span style={{ fontSize: 10, color: T.faint }}>{showWaiver ? '▲ hide' : '▼ show'}</span>
        </button>
        {showWaiver && (
          <div style={{ padding: '0 13px 13px', display: 'flex', flexWrap: 'wrap', gap: 8 }}>
            {['QB', 'RB', 'WR', 'TE'].map(function(pos) {
              const players = waiverByPos[pos] || [];
              if (!players.length) return null;
              return (
                <div key={pos} style={{ background: T.raised, border: '1px solid ' + T.border, borderTop: '2px solid ' + (POS_COLORS[pos] || T.border), borderRadius: '3px 3px 9px 9px', padding: '9px 11px', minWidth: 120, flex: '1 1 120px', maxWidth: 170 }}>
                  <div style={{ color: POS_COLORS[pos] || T.dim, fontSize: 10.5, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '0.07em', marginBottom: 6 }}>{pos}</div>
                  {players.map(function(p, i) {
                    return (
                      <div key={i} style={{ display: 'flex', justifyContent: 'space-between', fontSize: 12.5, padding: '3px 0', borderBottom: i < players.length - 1 ? '1px solid ' + T.border : 'none' }}>
                        <span style={{ color: T.text }}>{p.player.name}</span>
                        <span style={{ color: T.dim, fontSize: 11.5 }}>{p.redraftValue.toLocaleString()}</span>
                      </div>
                    );
                  })}
                </div>
              );
            })}
          </div>
        )}
      </div>

      <div style={{ padding: 15 }}>
        <div style={{ display: 'flex', flexWrap: 'wrap', gap: 11 }}>
          {[{ side: 'A', team: teamA, setTeam: setTeamA, text: sideAText, setText: setSideAText, accent: T.blue },
            { side: 'B', team: teamB, setTeam: setTeamB, text: sideBText, setText: setSideBText, accent: T.indigo }].map(function(cfg) {
            return (
              <div key={cfg.side} style={{ background: T.panel, border: '1px solid ' + T.border, borderTop: '3px solid ' + cfg.accent, borderRadius: '4px 4px 12px 12px', padding: 13, flex: 1, minWidth: 0 }}>
                <div style={{ display: 'flex', alignItems: 'center', gap: 7, marginBottom: 9 }}>
                  <span style={{ background: cfg.accent, color: '#fff', fontWeight: 700, fontSize: 12, width: 20, height: 20, borderRadius: 5, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>{cfg.side}</span>
                  <span style={{ color: T.dim, fontSize: 11, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '0.07em' }}>{'Side ' + cfg.side + ' gives up'}</span>
                </div>
                <select value={cfg.team} onChange={function(e) { cfg.setTeam(e.target.value); }} style={{ background: T.raised, color: T.text, border: '1px solid ' + T.borderHi, borderRadius: 8, padding: 10, width: '100%', marginBottom: 8, fontSize: 16 }}>
                  {teamOpts}
                </select>
                <textarea value={cfg.text} onChange={function(e) { cfg.setText(e.target.value); }} placeholder={"One player per line\ne.g. Ja'Marr Chase"} style={{ background: T.raised, color: T.text, border: '1px solid ' + T.borderHi, borderRadius: 8, padding: 11, width: '100%', minHeight: 92, fontSize: 16, resize: 'vertical', boxSizing: 'border-box', lineHeight: 1.5 }} />
              </div>
            );
          })}
        </div>

        <button onClick={gradeTrade} disabled={!sideAText.trim() && !sideBText.trim()} style={{ width: '100%', background: (!sideAText.trim() && !sideBText.trim()) ? T.raised : 'linear-gradient(135deg,' + T.indigo + ',' + T.indigoDk + ')', color: (!sideAText.trim() && !sideBText.trim()) ? T.faint : '#fff', border: 'none', borderRadius: 12, padding: 15, fontSize: 15, fontWeight: 700, letterSpacing: '1.5px', textTransform: 'uppercase', cursor: 'pointer', marginTop: 12 }}>
          Grade This Trade
        </button>

        {result && (
          <div style={{ marginTop: 16 }}>
            <div style={{ background: T.panel, border: '1px solid ' + T.border, borderRadius: 14, overflow: 'hidden', marginBottom: 12 }}>
              <div style={{ display: 'flex' }}>
                <div style={{ flex: 1, padding: '15px 10px', textAlign: 'center', background: winA ? 'rgba(16,185,129,0.08)' : 'transparent', borderRight: '1px solid ' + T.border }}>
                  <div style={{ fontSize: 10, color: T.faint, textTransform: 'uppercase', letterSpacing: '0.08em', marginBottom: 4 }}>SIDE A</div>
                  <div style={{ fontWeight: 700, fontSize: 32, color: winA ? T.green : T.text, lineHeight: 1 }}>{result.tA.toLocaleString()}</div>
                  {winA && <div style={{ fontSize: 10, color: T.green, fontWeight: 700, marginTop: 3, textTransform: 'uppercase' }}>▲ WINS</div>}
                </div>
                <div style={{ flex: 1, padding: '15px 10px', textAlign: 'center', background: winB ? 'rgba(16,185,129,0.08)' : 'transparent', borderLeft: '1px solid ' + T.border }}>
                  <div style={{ fontSize: 10, color: T.faint, textTransform: 'uppercase', letterSpacing: '0.08em', marginBottom: 4 }}>SIDE B</div>
                  <div style={{ fontWeight: 700, fontSize: 32, color: winB ? T.green : T.text, lineHeight: 1 }}>{result.tB.toLocaleString()}</div>
                  {winB && <div style={{ fontSize: 10, color: T.green, fontWeight: 700, marginTop: 3, textTransform: 'uppercase' }}>▲ WINS</div>}
                </div>
              </div>
              <div style={{ textAlign: 'center', padding: '10px 0', borderTop: '1px solid ' + T.border, background: T.panel2 }}>
                <span style={{ fontSize: 20 }}>{result.icon}</span>
                <span style={{ fontWeight: 800, fontSize: 12.5, color: T.amber, letterSpacing: '0.04em', marginLeft: 8 }}>{result.tier}</span>
                {result.winner !== 'even' && <span style={{ fontSize: 10.5, color: T.dim, marginLeft: 8 }}>{'by ' + result.gap.toLocaleString() + ' (' + result.gapPct.toFixed(0) + '%)'}</span>}
              </div>
            </div>

            <div style={{ display: 'flex', flexWrap: 'wrap', gap: 11, marginBottom: 12 }}>
              {[{ side: 'A', players: result.sA, team: teamA, accent: T.blue }, { side: 'B', players: result.sB, team: teamB, accent: T.indigo }].map(function(cfg) {
                return (
                  <div key={cfg.side} style={{ flex: 1, minWidth: 140, background: T.panel, border: '1px solid ' + T.border, borderRadius: 11, padding: '11px 13px' }}>
                    <div style={{ color: cfg.accent, fontSize: 11, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '0.05em', marginBottom: 6 }}>{teamLabel(cfg.team) + ' gives'}</div>
                    {cfg.players.length ? cfg.players.map(function(p, i) {
                      return (
                        <div key={i} style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '5px 0', borderBottom: '1px solid ' + T.border, fontSize: 13.5 }}>
                          <span style={{ color: p.found ? T.text : T.amber }}>{p.found ? p.officialName : p.input + ' ⚠'}</span>
                          <span style={{ color: p.found ? T.dim : T.amber, fontSize: 12.5, marginLeft: 8, fontWeight: 700 }}>{p.found ? p.value.toLocaleString() : 'no match'}</span>
                        </div>
                      );
                    }) : <div style={{ color: T.faint, fontSize: 12 }}>—</div>}
                  </div>
                );
              })}
            </div>

            {result.winner !== 'even' && result.addOns.length > 0 && (
              <div style={{ background: T.panel, border: '1px solid ' + T.border, borderLeft: '3px solid ' + T.amber, borderRadius: '4px 11px 11px 4px', padding: '11px 13px', marginBottom: 12 }}>
                <div style={{ color: T.amber, fontSize: 11, marginBottom: 6, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '0.05em' }}>{'To balance, ' + teamLabel(result.winner === 'A' ? teamA : teamB) + ' could add'}</div>
                {result.addOns.map(function(a, i) {
                  return <div key={i} style={{ display: 'flex', justifyContent: 'space-between', fontSize: 13.5, padding: '3px 0' }}><span>{a.name}</span><span style={{ color: T.dim }}>{a.value.toLocaleString()}</span></div>;
                })}
              </div>
            )}

            {Object.keys(result.waiverContext).length > 0 && (
              <div style={{ background: T.panel, border: '1px solid ' + T.border, borderRadius: 11, padding: '11px 13px', marginBottom: 12 }}>
                <div style={{ color: T.dim, fontSize: 11, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '0.06em', marginBottom: 8 }}>Waiver alternatives for traded positions</div>
                {Object.keys(result.waiverContext).map(function(pos) {
                  const players = result.waiverContext[pos];
                  return (
                    <div key={pos} style={{ marginBottom: 8 }}>
                      <span style={{ color: POS_COLORS[pos] || T.dim, fontSize: 11, fontWeight: 700, textTransform: 'uppercase' }}>{pos + ': '}</span>
                      {players.map(function(p, i) {
                        return <span key={i} style={{ fontSize: 13, color: T.text }}>{p.player.name + ' '}<span style={{ color: T.dim, fontSize: 12 }}>{'(' + p.redraftValue.toLocaleString() + ')'}</span>{i < players.length - 1 ? <span style={{ color: T.faint }}> · </span> : ''}</span>;
                      })}
                    </div>
                  );
                })}
              </div>
            )}

            <button onClick={getVerdict} disabled={vLoading} style={{ width: '100%', background: T.panel, border: '1px solid ' + T.amber, color: T.amber, borderRadius: 11, padding: '13px 20px', fontSize: 13.5, fontWeight: 700, letterSpacing: '0.5px', textTransform: 'uppercase', cursor: vLoading ? 'default' : 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 9, opacity: vLoading ? 0.7 : 1 }}>
              {vLoading ? <span style={spin} /> : "🎙"} The Commissioner's Verdict
            </button>

            {(verdict || vError) && (
              <div style={{ background: T.panel2, border: '1px solid ' + (vError ? T.amber : T.border), borderLeft: '3px solid ' + (vError ? T.amber : T.indigo), borderRadius: '4px 12px 12px 4px', padding: 14, color: vError ? T.amber : T.text, marginTop: 10, fontSize: 14, lineHeight: 1.6 }}>
                {vError ? 'The Commissioner is unavailable — give it another go.' : <MarkdownMessage text={verdict} />}
              </div>
            )}
          </div>
        )}
      </div>
    </div>
  );
}

// ── Live Scores (spoiler-safe) ───────────────────────────────────────────────
// Calls the Sleeper API live from the browser. Sleeper now returns
// access-control-allow-origin:* so there is no CORS problem (the old claude.md
// rule against browser calls is stale). Pick an NFL week; NOTHING is revealed
// by default — you tap each player whose real-life game you've already watched,
// and only then does that player's fantasy score appear. The matchup totals are
// running sums of what YOU have revealed, never the true team total, so the
// games you haven't watched yet are never spoiled.
const SLEEPER_BASE = 'https://api.sleeper.app/v1';
const LIVE_USERNAME = 'AlastairL';
const NFL_WEEKS = Array.from({ length: 18 }, function(_, i) { return i + 1; });

// Flatten rosters.json ("Team (@handle)" -> [{id,name,pos}]) into id -> {name,pos}.
function buildPlayerIndex(rostersData) {
  const idx = {};
  Object.values(rostersData || {}).forEach(function(arr) {
    (arr || []).forEach(function(p) { if (p && p.id != null) idx[String(p.id)] = { name: p.name, pos: p.pos }; });
  });
  return idx;
}

async function sget(url) {
  const r = await fetch(url);
  if (!r.ok) throw new Error('Sleeper ' + r.status);
  return r.json();
}

function fmtPts(n) { return (Math.round((n || 0) * 100) / 100).toFixed(2); }


function LiveTab(props) {
  const { rostersData } = props;
  const playerIdx = useMemo(function() { return buildPlayerIndex(rostersData); }, [rostersData]);

  const [base, setBase] = useState(null);     // { leagueId, leagueName, season, slots, teamByRoster, myRosterId, liveWeek }
  const [baseErr, setBaseErr] = useState(false);
  const [week, setWeek] = useState(null);
  const [pairs, setPairs] = useState(null);   // [{ mid, entries:[entry,...] }]
  const [loadingM, setLoadingM] = useState(false);
  const [matchErr, setMatchErr] = useState(false);
  const [viewMid, setViewMid] = useState(null);
  const [revealed, setRevealed] = useState({}); // `${rosterId}:${slotIdx}` -> true

  useEffect(function() {
    if (document.getElementById('live-spin')) return;
    const s = document.createElement('style');
    s.id = 'live-spin';
    s.textContent = '@keyframes live-spin{to{transform:rotate(360deg)}}';
    document.head.appendChild(s);
  }, []);

  // 1) one-time: NFL state + active Plancy league + users + rosters
  useEffect(function() {
    let cancelled = false;
    (async function() {
      try {
        const state = await sget(SLEEPER_BASE + '/state/nfl');
        const user = await sget(SLEEPER_BASE + '/user/' + LIVE_USERNAME);
        let league = null, usedSeason = null;
        const years = [String(state.season), String(+state.season - 1), String(+state.season - 2)];
        for (let i = 0; i < years.length; i++) {
          const leagues = await sget(SLEEPER_BASE + '/user/' + user.user_id + '/leagues/nfl/' + years[i]);
          const lg = (leagues || []).find(function(l) { return /borehamwood|plancy/i.test(l.name); });
          if (lg) { league = lg; usedSeason = years[i]; break; }
        }
        if (!league) throw new Error('No Plancy league found');
        const [users, rosters] = await Promise.all([
          sget(SLEEPER_BASE + '/league/' + league.league_id + '/users'),
          sget(SLEEPER_BASE + '/league/' + league.league_id + '/rosters'),
        ]);
        const userByUid = {};
        users.forEach(function(u) { userByUid[u.user_id] = { handle: u.display_name, team: (u.metadata && u.metadata.team_name) || u.display_name }; });
        const teamByRoster = {};
        let myRosterId = null;
        rosters.forEach(function(r) {
          const u = userByUid[r.owner_id] || {};
          teamByRoster[r.roster_id] = {
            handle: u.handle || null,
            name: u.handle ? displayName(u.handle) : ('Roster ' + r.roster_id),
            team: u.team || '',
            isMe: r.owner_id === user.user_id,
          };
          if (r.owner_id === user.user_id) myRosterId = r.roster_id;
        });
        const slots = (league.roster_positions || []).filter(function(p) { return p !== 'BN'; });
        const isLive = String(state.season) === String(usedSeason) && state.week > 0;
        if (cancelled) return;
        setBase({ leagueId: league.league_id, leagueName: league.name, season: usedSeason, slots: slots, teamByRoster: teamByRoster, myRosterId: myRosterId, liveWeek: isLive ? state.week : null });
        setWeek(isLive ? state.week : 1);
      } catch (e) { if (!cancelled) setBaseErr(true); }
    })();
    return function() { cancelled = true; };
  }, []);

  // 2) (re)load matchups for the selected week — resets reveals so a new week starts hidden
  function loadMatchups(b, wk) {
    if (!b || !wk) return;
    setLoadingM(true); setMatchErr(false);
    sget(SLEEPER_BASE + '/league/' + b.leagueId + '/matchups/' + wk).then(function(raw) {
      const byMid = {};
      (raw || []).forEach(function(e) { if (e.matchup_id == null) return; (byMid[e.matchup_id] = byMid[e.matchup_id] || []).push(e); });
      const list = Object.keys(byMid).map(function(mid) { return { mid: mid, entries: byMid[mid] }; });
      let mine = null;
      list.forEach(function(p) { if (p.entries.some(function(e) { return e.roster_id === b.myRosterId; })) mine = p.mid; });
      setPairs(list);
      setViewMid(mine || (list[0] && list[0].mid) || null);
      setRevealed({});
      setLoadingM(false);
    }).catch(function() { setMatchErr(true); setLoadingM(false); });
  }

  useEffect(function() { if (base && week) loadMatchups(base, week); }, [base, week]);

  function startersOf(entry, b) {
    return b.slots.map(function(slot, i) {
      const pid = entry.starters && entry.starters[i];
      const empty = !pid || pid === '0';
      const info = !empty ? playerIdx[String(pid)] : null;
      const pts = (entry.starters_points && entry.starters_points[i] != null)
        ? entry.starters_points[i]
        : (entry.players_points && entry.players_points[pid]) || 0;
      return { slot: slot, i: i, pid: pid, empty: empty, name: info ? info.name : (empty ? null : 'Player ' + pid), pos: info ? info.pos : null, pts: pts };
    });
  }

  function revealedTotal(entry, rows) {
    return rows.reduce(function(s, row) { return revealed[entry.roster_id + ':' + row.i] && !row.empty ? s + row.pts : s; }, 0);
  }
  function revealedCount(entry, rows) {
    return rows.reduce(function(n, row) { return revealed[entry.roster_id + ':' + row.i] && !row.empty ? n + 1 : n; }, 0);
  }

  function toggle(key) {
    setRevealed(function(r) { const n = Object.assign({}, r); if (n[key]) delete n[key]; else n[key] = true; return n; });
  }
  function setAll(entry, rows, on) {
    setRevealed(function(r) {
      const n = Object.assign({}, r);
      rows.forEach(function(row) { if (row.empty) return; const k = entry.roster_id + ':' + row.i; if (on) n[k] = true; else delete n[k]; });
      return n;
    });
  }

  const spin = { width: 16, height: 16, border: '2px solid ' + T.amber, borderTopColor: 'transparent', borderRadius: '50%', display: 'inline-block', animation: 'live-spin 0.8s linear infinite' };

  // ── error / loading shells ──
  if (baseErr) {
    return (
      <div style={{ background: T.bg, color: T.text, height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center', padding: 24, textAlign: 'center' }}>
        <div>
          <div style={{ fontSize: 30, marginBottom: 10 }}>📡</div>
          <div style={{ color: T.amber, fontWeight: 700, marginBottom: 6 }}>Live scores unavailable here</div>
          <div style={{ color: T.dim, fontSize: 13, lineHeight: 1.6, maxWidth: 340 }}>This artifact runs in a sandbox that blocks live calls to the Sleeper API, so real-time scores can't load. Stats, Banter and Trades still work — they use the built-in league data.</div>
        </div>
      </div>
    );
  }
  if (!base) {
    return (
      <div style={{ background: T.bg, color: T.text, height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 11 }}>
        <span style={spin} /><span style={{ color: T.dim, fontSize: 14 }}>Connecting to Sleeper…</span>
      </div>
    );
  }

  const pair = (pairs || []).find(function(p) { return p.mid === viewMid; });
  let entries = pair ? pair.entries.slice() : [];
  entries.sort(function(a, b) { return (a.roster_id === base.myRosterId ? -1 : 0) - (b.roster_id === base.myRosterId ? -1 : 0); });

  function teamLabelOf(rid) { const t = base.teamByRoster[rid] || {}; return t.team ? (t.name + ' · ' + t.team) : t.name; }

  return (
    <div style={{ background: T.bg, color: T.text, overflowY: 'auto', height: '100%', boxSizing: 'border-box' }}>
      {/* controls */}
      <div style={{ position: 'sticky', top: 0, zIndex: 2, background: T.panel, borderBottom: '1px solid ' + T.border, padding: '11px 14px' }}>
        <div style={{ display: 'flex', gap: 9, alignItems: 'center' }}>
          <div style={{ flex: '0 0 auto' }}>
            <div style={{ fontSize: 9.5, color: T.faint, textTransform: 'uppercase', letterSpacing: '0.08em', marginBottom: 3, fontWeight: 700 }}>Week</div>
            <select value={week} onChange={function(e) { setWeek(+e.target.value); }} style={{ background: T.raised, color: T.text, border: '1px solid ' + T.borderHi, borderRadius: 8, padding: '8px 10px', fontSize: 15 }}>
              {NFL_WEEKS.map(function(w) { return <option key={w} value={w}>{'Week ' + w + (w === base.liveWeek ? ' • now' : '')}</option>; })}
            </select>
          </div>
          <div style={{ flex: 1, minWidth: 0 }}>
            <div style={{ fontSize: 9.5, color: T.faint, textTransform: 'uppercase', letterSpacing: '0.08em', marginBottom: 3, fontWeight: 700 }}>Matchup</div>
            <select value={viewMid || ''} onChange={function(e) { setViewMid(e.target.value); }} style={{ background: T.raised, color: T.text, border: '1px solid ' + T.borderHi, borderRadius: 8, padding: '8px 10px', fontSize: 15, width: '100%' }}>
              {(pairs || []).map(function(p) {
                const nm = p.entries.map(function(e) { const t = base.teamByRoster[e.roster_id] || {}; return t.name + (t.isMe ? ' (you)' : ''); }).join('  vs  ');
                return <option key={p.mid} value={p.mid}>{nm}</option>;
              })}
            </select>
          </div>
          <button onClick={function() { loadMatchups(base, week); }} title="Refresh live points" style={{ flex: '0 0 auto', alignSelf: 'flex-end', background: T.raised, border: '1px solid ' + T.borderHi, color: T.dim, borderRadius: 8, padding: '8px 11px', fontSize: 14, cursor: 'pointer' }}>↻</button>
        </div>
        <div style={{ fontSize: 11, color: T.faint, marginTop: 8, lineHeight: 1.45 }}>
          Tap a player whose game you've <em>already watched</em> to reveal their score. Nothing is shown otherwise — unwatched games stay unspoiled.
        </div>
      </div>

      {loadingM && (
        <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 11, padding: 40 }}>
          <span style={spin} /><span style={{ color: T.dim, fontSize: 14 }}>{'Loading week ' + week + '…'}</span>
        </div>
      )}

      {!loadingM && matchErr && (
        <div style={{ padding: 30, textAlign: 'center', color: T.amber, fontSize: 14 }}>Couldn't load this week's matchups — try ↻.</div>
      )}

      {!loadingM && !matchErr && (!pairs || !pairs.length) && (
        <div style={{ padding: 30, textAlign: 'center', color: T.dim, fontSize: 14 }}>{'No matchups posted for week ' + week + ' yet.'}</div>
      )}

      {!loadingM && !matchErr && pair && (
        <div style={{ padding: 14 }}>
          {/* running scoreboard — revealed totals only */}
          <div style={{ display: 'flex', background: T.panel, border: '1px solid ' + T.border, borderRadius: 14, overflow: 'hidden', marginBottom: 14 }}>
            {entries.map(function(entry, idx) {
              const rows = startersOf(entry, base);
              const tot = revealedTotal(entry, rows);
              const cnt = revealedCount(entry, rows);
              const playable = rows.filter(function(r) { return !r.empty; }).length;
              const t = base.teamByRoster[entry.roster_id] || {};
              return (
                <div key={entry.roster_id} style={{ flex: 1, padding: '13px 10px', textAlign: 'center', borderRight: idx === 0 ? '1px solid ' + T.border : 'none', background: t.isMe ? 'rgba(99,102,241,0.07)' : 'transparent' }}>
                  <div style={{ fontSize: 10.5, color: t.isMe ? T.indigo : T.faint, textTransform: 'uppercase', letterSpacing: '0.06em', marginBottom: 4, fontWeight: 700, whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis' }}>{t.name}{t.isMe ? ' (you)' : ''}</div>
                  <div style={{ fontWeight: 800, fontSize: 30, color: cnt ? T.text : T.faint, lineHeight: 1 }}>{fmtPts(tot)}</div>
                  <div style={{ fontSize: 10.5, color: T.dim, marginTop: 4 }}>{cnt + '/' + playable + ' revealed'}</div>
                </div>
              );
            })}
          </div>

          {/* per-team starter lists */}
          {entries.map(function(entry) {
            const rows = startersOf(entry, base);
            const t = base.teamByRoster[entry.roster_id] || {};
            const cnt = revealedCount(entry, rows);
            const playable = rows.filter(function(r) { return !r.empty; }).length;
            const allOn = cnt === playable && playable > 0;
            return (
              <div key={entry.roster_id} style={{ background: T.panel, border: '1px solid ' + T.border, borderTop: '3px solid ' + (t.isMe ? T.indigo : T.border), borderRadius: '4px 4px 12px 12px', padding: '12px 13px', marginBottom: 12 }}>
                <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 9 }}>
                  <div style={{ minWidth: 0 }}>
                    <div style={{ fontWeight: 700, fontSize: 14, color: T.text, whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis' }}>{teamLabelOf(entry.roster_id)}{t.isMe ? <span style={{ color: T.indigo, fontSize: 10.5, fontWeight: 800, marginLeft: 6 }}>YOU</span> : null}</div>
                  </div>
                  <button onClick={function() { setAll(entry, rows, !allOn); }} style={{ flex: '0 0 auto', background: 'none', border: '1px solid ' + T.borderHi, color: T.dim, borderRadius: 7, padding: '5px 9px', fontSize: 10.5, fontWeight: 700, textTransform: 'uppercase', letterSpacing: '0.05em', cursor: 'pointer' }}>{allOn ? 'Hide all' : 'Reveal all'}</button>
                </div>
                {rows.map(function(row) {
                  const key = entry.roster_id + ':' + row.i;
                  const isOpen = !!revealed[key];
                  const tint = POS_COLORS[row.slot] || T.dim;
                  if (row.empty) {
                    return (
                      <div key={row.i} style={{ display: 'flex', alignItems: 'center', gap: 9, padding: '7px 0', borderBottom: '1px solid ' + T.border }}>
                        <span style={{ flex: '0 0 38px', fontSize: 10, fontWeight: 800, color: T.faint, textTransform: 'uppercase' }}>{row.slot}</span>
                        <span style={{ flex: 1, color: T.faint, fontSize: 13, fontStyle: 'italic' }}>Empty</span>
                      </div>
                    );
                  }
                  return (
                    <div key={row.i} onClick={function() { toggle(key); }} style={{ display: 'flex', alignItems: 'center', gap: 9, padding: '7px 0', borderBottom: '1px solid ' + T.border, cursor: 'pointer' }}>
                      <span style={{ flex: '0 0 38px', fontSize: 10, fontWeight: 800, color: tint, textTransform: 'uppercase' }}>{row.slot}</span>
                      <span style={{ flex: 1, minWidth: 0, color: isOpen ? T.text : T.dim, fontSize: 13.5, whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis' }}>
                        {row.name}{row.pos ? <span style={{ color: T.faint, fontSize: 11, marginLeft: 6 }}>{row.pos}</span> : null}
                      </span>
                      {isOpen
                        ? <span style={{ flex: '0 0 auto', fontWeight: 800, fontSize: 14.5, color: row.pts > 0 ? T.green : T.faint, minWidth: 48, textAlign: 'right' }}>{fmtPts(row.pts)}</span>
                        : <span style={{ flex: '0 0 auto', fontSize: 10.5, fontWeight: 700, color: T.indigo, border: '1px solid ' + T.borderHi, borderRadius: 20, padding: '4px 11px', textTransform: 'uppercase', letterSpacing: '0.05em' }}>Reveal</span>}
                    </div>
                  );
                })}
              </div>
            );
          })}
        </div>
      )}
    </div>
  );
}

const CSS = `
  *,*::before,*::after{box-sizing:border-box;margin:0;padding:0;}
  html,body{height:100%;}
  body{background:#090d18;-webkit-font-smoothing:antialiased;overflow:hidden;}
  ::-webkit-scrollbar{width:5px;height:5px;}
  ::-webkit-scrollbar-thumb{background:#24344e;border-radius:3px;}
  @keyframes ld-pulse{0%,80%,100%{opacity:.2;transform:scale(.7)}40%{opacity:1;transform:scale(1)}}
  .ld{display:inline-block;width:7px;height:7px;background:#6366f1;border-radius:50%;margin:0 2px;animation:ld-pulse 1.4s infinite ease-in-out both;}
  .ld:nth-child(2){animation-delay:.2s}.ld:nth-child(3){animation-delay:.4s}
  .md-table{border-collapse:collapse;width:100%;font-size:13px;background:#0f1625;border-radius:10px;overflow:hidden;}
  .md-table th{text-align:left;padding:9px 12px;background:#090d18;color:#f59e0b;font-weight:800;text-transform:uppercase;font-size:10.5px;letter-spacing:0.07em;border-bottom:1px solid #24344e;white-space:nowrap;}
  .md-table td{padding:8px 12px;border-bottom:1px solid #1a2640;color:#e8f1ff;}
  .md-table td:first-child{font-weight:600;}
  .md-table tr:last-child td{border-bottom:none;}
  .md-table tbody tr:nth-child(even) td{background:rgba(99,102,241,0.04);}
  .chip{background:#1a2640;border:1px solid #304260;color:#e8f1ff;border-radius:20px;padding:8px 15px;font-size:13px;font-weight:600;cursor:pointer;white-space:nowrap;flex-shrink:0;transition:border-color .15s,background .15s;}
  .chip:hover:not(:disabled){border-color:#6366f1;background:#232e50;}
  .chip:disabled{opacity:0.4;cursor:default;}
  .send-btn{background:linear-gradient(135deg,#6366f1,#4f46e5);color:#fff;border:none;border-radius:12px;padding:0 18px;font-weight:700;font-size:12.5px;letter-spacing:0.8px;text-transform:uppercase;cursor:pointer;align-self:stretch;flex-shrink:0;}
  .send-btn:disabled{background:#1a2640;color:#3d5470;cursor:default;}
  .nav-btn{flex:1;display:flex;flex-direction:column;align-items:center;gap:3px;padding:10px 4px;background:transparent;border:none;cursor:pointer;position:relative;}
  .nav-btn .nav-ico{font-size:20px;}
  .nav-btn:not(.active) .nav-ico{filter:grayscale(0.7) opacity(0.5);}
  .nav-lab{font-size:10px;font-weight:800;letter-spacing:0.1em;text-transform:uppercase;}
  .plaincy-app{display:flex;flex-direction:column;height:var(--app-h,100dvh);width:100%;max-width:560px;margin:0 auto;background:#090d18;overflow:hidden;}
  @media(min-width:640px){.plaincy-app{max-width:780px;}}
  button{font-family:inherit;}
  textarea,select{font-family:inherit;font-size:16px;}
  textarea:focus,select:focus{outline:none;}
  select option{background:#141e32;color:#e8f1ff;}
`;

const TABS = [
  { id: 'stats', icon: '📊', label: 'STATS' },
  { id: 'banter', icon: '🎙', label: 'BANTER' },
  { id: 'trade', icon: '⚖️', label: 'TRADES' },
  { id: 'live', icon: '🏈', label: 'SCORES' },
];

export default function App() {
  const [tab, setTab] = useState('stats');
  const { history, stats, rosters, trades, alltime, transactions, players, live } = useLeagueData();
  const { keyboardOpen } = useViewport();
  const lore = useLore(tab === 'banter');

  useEffect(function() {
    if (document.getElementById('plaincy-css')) return;
    const el = document.createElement('style');
    el.id = 'plaincy-css';
    el.textContent = CSS;
    document.head.appendChild(el);
  }, []);

  return (
    <div className="plaincy-app">
      <div style={{ height: 3, background: 'linear-gradient(90deg,#6366f1 0%,#f59e0b 60%,#6366f1 100%)', flexShrink: 0 }} />
      <div style={{ display: 'flex', alignItems: 'center', gap: 13, padding: '12px 16px', background: T.panel, borderBottom: '1px solid ' + T.border, flexShrink: 0 }}>
        <div style={{ width: 40, height: 40, borderRadius: 12, background: 'linear-gradient(135deg,#6366f1,#4f46e5)', display: 'flex', alignItems: 'center', justifyContent: 'center', fontWeight: 900, fontSize: 22, color: '#fff', flexShrink: 0 }}>P</div>
        <div style={{ flex: 1, minWidth: 0 }}>
          <div style={{ fontWeight: 900, fontSize: 22, color: T.text, letterSpacing: '-0.5px', lineHeight: 1 }}>Pl<span style={{ color: T.indigo }}>AI</span>ncy</div>
          <div style={{ fontSize: 11, color: T.dim, marginTop: 2, letterSpacing: '0.08em', textTransform: 'uppercase', fontWeight: 600 }}>Borehamwood Plancy League</div>
        </div>
        <div title={live ? 'Fetched the latest published data' : 'Running on the data baked into this artifact — re-paste to update'} style={{ fontSize: 10.5, color: T.dim, border: '1px solid ' + T.border, borderRadius: 6, padding: '4px 9px', whiteSpace: 'nowrap', flexShrink: 0, fontWeight: 600, display: 'flex', alignItems: 'center', gap: 5 }}>
          <span style={{ width: 6, height: 6, borderRadius: '50%', background: live ? T.green : T.amber, display: 'inline-block' }} />
          {(live ? 'Live · ' : 'Snapshot · ') + fmtBuiltAt(BUILT_AT)}
        </div>
      </div>

      <div style={{ flex: 1, overflow: 'hidden', display: 'flex', flexDirection: 'column' }}>
        {/* Stats / Banter / Trade stay MOUNTED (toggle display) so chat history and
            trade inputs survive tab switches — storage APIs throw in the artifact
            sandbox, so keep-alive is the only option. Live mounts on demand: it
            fetches Sleeper on mount and is spoiler-safe, so a fresh mount is correct. */}
        <div style={{ display: tab === 'stats' ? 'flex' : 'none', flex: 1, minHeight: 0, flexDirection: 'column' }}>
          <StatsTab historyData={history} statsData={stats} alltimeData={alltime} transactionsData={transactions} playersData={players} loreMaster={lore.master} />
        </div>
        <div style={{ display: tab === 'banter' ? 'flex' : 'none', flex: 1, minHeight: 0, flexDirection: 'column' }}>
          <BanterTab historyData={history} statsData={stats} alltimeData={alltime} lore={lore} />
        </div>
        <div style={{ display: tab === 'trade' ? 'flex' : 'none', flex: 1, minHeight: 0, flexDirection: 'column' }}>
          <TradeGrader rostersData={rosters} tradeValues={trades} />
        </div>
        {tab === 'live' && <LiveTab rostersData={rosters} />}
      </div>

      {!keyboardOpen && (
        <nav style={{ display: 'flex', background: T.panel, borderTop: '1px solid ' + T.border, flexShrink: 0, paddingBottom: 'env(safe-area-inset-bottom)' }}>
          {TABS.map(function(t) {
            const active = tab === t.id;
            return (
              <button key={t.id} onClick={function() { setTab(t.id); }} className={'nav-btn' + (active ? ' active' : '')} style={{ color: active ? T.text : T.faint }}>
                {active && <span style={{ position: 'absolute', top: 0, left: '50%', transform: 'translateX(-50%)', width: 36, height: 3, borderRadius: '0 0 4px 4px', background: T.indigo }} />}
                <span className="nav-ico">{t.icon}</span>
                <span className="nav-lab">{t.label}</span>
              </button>
            );
          })}
        </nav>
      )}
    </div>
  );
}
